# Activity Executions First-Class Placeholder

This placeholder tracks the future implementation of Activity Executions as first-class durable objects identified by `temporal-api-v1.62-sync`. That future spec will implement the eight Activity Executions RPCs and define the kernel representation of pending activities as durable, addressable objects.
