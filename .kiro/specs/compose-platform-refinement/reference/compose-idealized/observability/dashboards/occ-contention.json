{
  "uid": "tokeira-occ-contention",
  "title": "OCC Contention",
  "schemaVersion": 39,
  "version": 1,
  "refresh": "15s",
  "tags": ["tokeira", "dsql", "occ"],
  "timezone": "browser",
  "templating": {
    "list": [
      {
        "name": "datasource",
        "type": "datasource",
        "query": "prometheus",
        "current": {
          "selected": true,
          "text": "Mimir",
          "value": "mimir"
        }
      }
    ]
  },
  "panels": [
    {
      "id": 1,
      "type": "row",
      "title": "Conflicts",
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 0 }
    },
    {
      "id": 2,
      "type": "timeseries",
      "title": "OCC Conflicts/s",
      "datasource": { "type": "prometheus", "uid": "mimir" },
      "targets": [
        {
          "expr": "sum by (operation) (rate(tokeira_storage_dsql_occ_conflict_total[5m]))",
          "legendFormat": "{{operation}}"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "ops",
          "custom": {
            "lineInterpolation": "smooth",
            "showPoints": "never",
            "pointSize": 0
          }
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "table",
          "placement": "bottom",
          "calcs": ["lastNotNull", "mean", "max"]
        }
      },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 1 },
      "description": "Serialization conflicts grouped by bounded storage operation label."
    }
  ]
}
