# Kernel Pause Workflow Placeholder

This placeholder tracks the future implementation of first-class workflow pause and unpause support identified by `temporal-api-v1.62-sync`. That future spec will introduce kernel transitions for `PauseWorkflowExecution` and `UnpauseWorkflowExecution`, distinct from the v1.43 activity-level pause-by-id surface.
