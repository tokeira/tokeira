# Nexus Multi-Cluster Placeholder

This placeholder tracks the future Nexus multi-cluster endpoint policy identified by `temporal-api-v1.62-sync`. That future spec will reintroduce `NexusEndpointSpec.allowed_cluster_ids` with typed endpoint policy and cross-cluster routing semantics.
