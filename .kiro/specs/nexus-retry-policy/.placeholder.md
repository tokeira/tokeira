# Nexus Retry Policy Placeholder

This placeholder tracks the future Nexus retry-policy implementation identified by `temporal-api-v1.62-sync`. That future spec will reintroduce `RespondNexusTaskFailedRequest.error.retry_behavior` with typed DTO support and runtime `NexusTaskBroker` retry branching.
