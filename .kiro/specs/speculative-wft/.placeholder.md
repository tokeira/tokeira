# Speculative Workflow Task Placeholder

This placeholder tracks the future speculative workflow task implementation identified by `temporal-api-v1.62-sync`. That future spec will consume the edge-decoded `client_discards_speculative_with_events` capability and add downstream speculative workflow task behavior.
