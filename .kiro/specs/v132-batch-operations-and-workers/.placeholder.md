# v132-batch-operations-and-workers

Owner: D2; Claude orchestrates the spec, the integration seat approves it, and Codex implements it.

Scope: `BatchOperationType` migration; `target_executions`; `ListWorkers` system-worker exclusion; `CountWorkers`; `PollerGroupsInfo`; `TaskQueueStats.rate_limiting_active`; `EnvironmentInfo`; poller auto-enroll capability.

Corpus anchors (Temporal server @ `v1.32.0`):

- `tests/workflow_api_batch_cancel_test.go`
- `tests/worker_registry_test.go`

This placeholder is replaced by the full spec (`requirements.md`, `design.md`, and
`tasks.md`) after its review; it does not authorize behavior changes.
