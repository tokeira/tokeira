# v132-gated-surfaces

Owner: D7; Claude orchestrates the spec, the integration seat approves it, and Codex implements it.

Scope: time skipping as Stock_Rejection plus `PollWorkflowExecutionTimeSkipping`; WFT completion pagination shapes; worker commands; external storage reference and event-group markers.

Corpus anchors (Temporal server @ `v1.32.0`):

- `tests/timeskipping_test.go`
- `tests/timeskipping_fast_forward_test.go`
- `tests/timeskipping_propagation_test.go`
- `tests/workflow_completion_pagination_test.go`
- `tests/worker_commands_task_test.go`

This placeholder is replaced by the full spec (`requirements.md`, `design.md`, and
`tasks.md`) after its review; it does not authorize behavior changes.
