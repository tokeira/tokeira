# v132-lifecycle-fidelity

Owner: D6; Claude orchestrates the spec, the integration seat approves it, and Codex implements it.

Scope: start-child conflict policy; reuse-policy migration; pause bypass fix; `StartedClock` on retry; eager activity execution stock-on; error-message truncation; `first_execution_run_id`; response `link`s; gated-off update callbacks, signal backlinks, SignalWithStart-from-workflow; schedule duration validation and scheduler-v2 fields.

Corpus anchors (Temporal server @ `v1.32.0`):

- `tests/child_workflow_test.go`
- `tests/signal_with_start_from_workflow_test.go`
- `tests/schedule_test.go`

This placeholder is replaced by the full spec (`requirements.md`, `design.md`, and
`tasks.md`) after its review; it does not authorize behavior changes.
