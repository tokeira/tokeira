# v132-nexus

Owner: D5; Claude orchestrates the spec, the integration seat approves it, and Codex implements it.

Scope: callback routing by URL scheme; `callback.allowedAddresses`; standalone Nexus operations as Stock_Rejection; SignalWithStart system endpoint; endpoint-not-found; metadata validation; `state_size_bytes`.

Corpus anchors (Temporal server @ `v1.32.0`):

- `tests/nexus_api_test.go`
- `tests/nexus_standalone_test.go`
- `tests/nexus_workflow_update_test.go`

This placeholder is replaced by the full spec (`requirements.md`, `design.md`, and
`tasks.md`) after its review; it does not authorize behavior changes.
