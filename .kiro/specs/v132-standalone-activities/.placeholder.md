# v132-standalone-activities

Owner: D1; Claude orchestrates the spec, the integration seat approves it, and Codex implements it.

Scope: Standalone Activities GA and stock-on; start delay; `ACTIVITY_EXECUTION_STATUS_PAUSED`; the four `*ActivityExecution` operator RPCs; activity batch operations as Stock_Rejection; absorbs the open tasks of `activity-executions-first-class`.

Corpus anchors (Temporal server @ `v1.32.0`):

- `tests/activity_standalone_test.go`
- `tests/activity_api_pause_test.go`
- `tests/activity_api_batch_cancel_test.go`

This placeholder is replaced by the full spec (`requirements.md`, `design.md`, and
`tasks.md`) after its review; it does not authorize behavior changes.
