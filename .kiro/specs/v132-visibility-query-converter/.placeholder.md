# v132-visibility-query-converter

Owner: D3; Claude orchestrates the spec, the integration seat approves it, and Codex implements it.

Scope: unified query converter semantics.

Corpus anchors (Temporal server @ `v1.32.0`):

- `tests/advanced_visibility_test.go`
- `tests/workflow_visibility_test.go`

This placeholder is replaced by the full spec (`requirements.md`, `design.md`, and
`tasks.md`) after its review; it does not authorize behavior changes.
