# v132-worker-deployments

Owner: D4; Claude orchestrates the spec, the integration seat approves it, and Codex implements it.

Scope: one-time override; child versioning override; reactivation signals stock-on; demotion signal; dedup, delete-missing, trampolining fixes; error-text change; V1/V2 final-release posture verified unchanged.

Corpus anchors (Temporal server @ `v1.32.0`):

- `tests/worker_deployment_test.go`
- `tests/worker_deployment_version_test.go`

This placeholder is replaced by the full spec (`requirements.md`, `design.md`, and
`tasks.md`) after its review; it does not authorize behavior changes.
