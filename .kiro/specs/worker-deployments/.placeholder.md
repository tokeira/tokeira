# Worker Deployments Placeholder

This placeholder tracks the future implementation of the 11 Worker Deployments RPCs and the `temporal.api.deployment.v1` messages identified by `temporal-api-v1.62-sync`. That future spec will replace the deferred stubs, implement versioning and deployment-routing semantics, and flip `SystemCapabilities.server_scaled_deployments` from `false` to `true`.
