# Worker Heartbeat Observability Placeholder

This placeholder tracks the future worker heartbeat observability feature identified by `temporal-api-v1.62-sync`. That future spec will persist `WorkerHeartbeat` records, expose worker liveness, add metrics, provide the `ListWorkers` projection, and promote `record_worker_heartbeat` from no-op to a real handler.
