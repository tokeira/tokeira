# Gitignored files to copy into every new worktree. Gitignore glob syntax.
# Read natively by Claude Code and the ChatGPT app (Codex); applied by `tkw`
# for the worktrees it creates (Kiro CLI, manual).
#
# Add local-only config that builds/tests need as it appears.
.env
.env.local
# Machine-local Claude Code grants (absolute-path allow rules survive into worktrees)
.claude/settings.local.json
# Machine-local Claude Code imports (e.g. the operator's private-ops pointer);
# copied so fresh worktrees inherit the operator's local file.
CLAUDE.local.md
