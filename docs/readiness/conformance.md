# Conformance Readiness — Temporal v1.31.0

> **This is the single status surface for how far tokeira
> has progressed toward Temporal v1.31.0 compliance.** It is the *numerator*. The *denominator* — what
> full v1.31.0 compliance **means** — is defined in [`../conformance/v1.31.0/`](../conformance/v1.31.0/README.md).
>
> Supersedes the status tables previously scattered across `temporal-functional-conformance/reference/
> FINDINGS.md` (its Status ledger) and `api-conformance-tracker/tracker.md` (its progress counts). Those
> remain as **detail-by-reference**: FINDINGS for the deep per-cluster investigation + Implementer
> Mandate; tracker for the RPC→spec index.

**Target:** `TEMPORAL_SERVER_COMPAT = 1.31.0`, proto `v1.62.11` (`crates/tokeira-build-info/src/pinned.rs`).
**Last updated:** 2026-07-25 · **Contributors for status:** Kiro, Claude, Codex.

## How to read this

Every area carries one of three honest states — we do **not** assert compliance we have not measured:

- ✅ **Verified** — exercised and passing (unit/property/golden, or the Tier-2 functional corpus, or a
  cited end-to-end run). Evidence noted.
- 🟡 **Implemented, unverified** — code exists and the happy path works, but the full conformance
  surface has not been measured against the corpus.
- ⬜ **Outstanding** — not implemented / not started.
- ⏸ **Deferred** · ⛔ **Out of public scope** (depends on an internal surface tokeira does not front).

A green check in code is not compliance; a measured pass against v1.31.0 ground truth is. (See the
Implementer Mandate in FINDINGS — "a wrong guess behind a green check bakes in non-conformance.")

## The three conformance tiers

| Tier | What it proves | Owning spec | State |
|------|----------------|-------------|:-----:|
| Compatibility surface/metadata | The claim is explicit, queryable, pinned | `temporal-compatibility`, `temporal-compatibility-surface` | 🟡 |
| Tier-1 in-process oracle | Responses + history match v1.31.0, RPC coverage gate | `conformance-harness` (`tokeira-conformance` crate) | ⬜ not built |
| Tier-2 functional corpus | Temporal's own Go suites pass over gRPC | `temporal-functional-conformance` | 🟡 partial |

## Tier-2 functional corpus — cluster status

From the canonical run (corpus @ v1.31.0; 100 entrypoints; 1501 per-test outcomes: 1194 fail / 267
unfinished / 19 pass / 21 skip at the 2026-06-09 baseline, then targeted fixes below).

| Cluster | Area | State | Measured | What remains |
|---------|------|:-----:|:--------:|--------------|
| C4a | Nexus endpoint admin CRUD | ✅ | Tier 7.35: 54 corpus outcomes pass, 0 fail, 0 skip; 13 edge tests | Author proptests P1–P4. |
| C5a | Completion-callback admission validation | ✅ | `TestCallbacksSuiteHSM` validation leaves 6/6 green twice | Conformance overrides exercise `allowedAddresses`; production address-policy configuration remains an explicit bounded deviation. |
| C5b | Other admission validators (links, …) | ✅ | done | Residuals driven by a corpus re-run. |
| C6 | Over-rejection (cron, nil/empty SA+memo) | ✅ | done | Full corpus re-run pending. |
| C1 | Standalone / first-class activity RPCs | 🟡 | **1 pass / 31 fail** | SA admission validation, Describe fidelity, count-by-id; chasm-activity timeout/retry (~20 tests) needs a spec. Owned by `activity-executions-first-class`. |
| C2 | Worker deployment / versioning | ✅ | Tier 8.39 `TestWorkerDeploymentSuite`: 57 pass / 0 fail / 3 corpus-native skips; Tier 8.40 `TestDeploymentVersionSuite`: 64 active methods pass / 0 fail / 1 exact classified exclusion; Tier 8.41 `TestVersioning3FunctionalSuite`: 319 pass / 0 fail / 4 corpus-native skips plus 33 exact internal-topology exclusions; Tier 8.42 `TestWorkerRegistryTestSuite`: 7 pass / 0 fail / 0 skip; all clean twice | The 406-test `TestVersioningFunctionalSuite` enabled path is out of surface per the resolved V1/V2 decision (`.kiro/specs/worker-deployments/reference/v1-v2-conformance-decision.md`); stock-default rejection behavior is covered separately. |
| C3 | Visibility list/query + search attributes | ✅ | Advanced Visibility unified + legacy: 40 active leaves pass, 0 fail, 2 corpus-native skips; 22 deprecated enabled-path exclusions | Tier 4.24 clean twice consecutively in both converter modes. `ORDER BY` self-skips because the corpus limits it to Elasticsearch. The 11 V1/V2 enabled-path methods per mode are excluded by the resolved stock-default Worker Versioning decision; shared `WorkerVersionStamp`/`BuildIds` fidelity remains active and passes. |
| C4b | Nexus operation execution / task transport | ✅ | Tier 7.37 `TestNexusWorkflowTestSuite` clean twice; Tier 7.38 both `TestNexusApiTestSuite` failure modes clean twice with 39 outcomes each, 0 fail | Full corpus re-run pending. |
| C7 | Lifecycle / describe `NotFound` | ⏸ | — | Re-triage after C1–C4 (many are cascades). |
| C9 | `unfinished` panic siblings | ⬜ | 0/267 | Fix the entrypoint panic; 267 siblings then resolve to real pass/fail. |
| C8 | Internal-surface / admin-service tests | ⛔ | — | Out of public scope by construction. |

**Biggest unknown (unmeasured denominator):** C9 (267 unfinished). Until these are measured, the
overall conformance percentage is **not** known — establishing that real denominator is the next
audit priority.

**Order of attack:** C1 → C3 (cheap re-run) → C9 (panic fix, reclassifies 267) → C7 (re-triage).
Done: C2, C4a, C4b, C5a, C5b, C6.

## Drive-to-green ledger (suite-by-suite, per `functional-test-order.md`)

Fix-to-green campaign (`docs/HANDOVER-functional-conformance.md`, retired to git history): a suite is **clean** when every
test is green or a classified skip with a cited registry reason — zero unclassified non-pass.

| Tier | Suite | Result | Date | Notes |
|------|-------|--------|------|-------|
| 10.45 | `TestPrioritySuite` + `TestFairnessSuite` + `TestFairnessAutoEnableSuite` | ✅ **CLEAN — 4 active leaves pass / 0 fail + 1 corpus-native skip + 8 exact classified leaves** (2× consecutive fresh-process runs; sticky leaf also passes in isolation) | 2026-07-26 | Ground truth from `common/dynamicconfig/constants.go`, `service/matching/task_queue_partition_manager.go`, `service/matching/fair_backlog_manager.go`, `service/matching/fair_level.go`, `service/matching/fairness_util.go`, `service/matching/fairness.md`, `service/frontend/workflow_handler.go`, and `tests/priority_fairness_test.go @ v1.31.0` established the public behavior and stock defaults: priority-aware delivery on, five bands/default key 3, User Fairness and auto-enable off, sticky excluded from fairness, and default workflow-ID conflict policy FAIL. Tokeira implements that behavior in its delivery plane rather than introducing Temporal matching/history service objects. The pure stateless kernel only copies captured Priority metadata into deterministic dispatch intent and retains no scheduler state; runtime owns effective ordering, weighted stride, sticky/normal arbitration, stale-offer fencing, live config, and queue/per-key rate shaping; storage persists an explicit `(priority_key, fair_pass, insertion_seq)` delivery order; the edge validates and projects inheritance, updates, config, and real `stats_by_priority_key`. The configuration-policy follow-on replaced the campaign's volatile `TaskQueueConfigStore` with a dedicated kind-isolated CAS repository: updates commit before success, server caches hydrate before traffic, and process replacement recovers the last committed rates and weight overrides. The Shape-2 fork adds a read-only Admin observation adapter backed by public `DescribeTaskQueue`, never edits `test_env.go` or a corpus test body, and uses a 100-case mapping/classification property. Exact classifications: six leaves assert Temporal's classic/priority/fair matcher migration topology, one non-auto pending-task invalidation leaf asserts unavailable in-process matching-client metrics, and the sticky leaf deterministically reuses live workflow IDs left by its predecessor under default FAIL; the unmodified sticky leaf passes alone on a fresh process and is also pinned by delivery properties. The auto-enable pending-task leaf self-skips in upstream v1.31.0 as flaky. Each final pass invoked the runner separately with the exact suite patterns `'^TestPrioritySuite$'`, `'^TestFairnessSuite$'`, and `'^TestFairnessAutoEnableSuite$'`: tallies were respectively 3/0/0, 2/0/0, and 2/0/1 on both passes; wrongorderness was 0 and unfairness 0.45. The durable configuration-policy follow-on repeated those three exact invocations twice on 2026-07-26 with the same tallies and measurements. It also repeated the exact `TestUpdateAndDescribeTaskQueueConfig`, `TestUpdateUnsetAndDescribeTaskQueueConfig`, and `TestTaskQueueRateLimit_UpdateFromWorkerConfigAndAPI` leaves twice over the real wire; each runner tally was 2/0/0 on both passes. The original priority validation used Tokeira `3a4b7563` and Temporal `3fa03f7a`; the durability follow-on used working trees based on Tokeira `ff372a2e` and Temporal `423ae614`. The complete fork package compiles with `go test -tags test_dep ./tests -run '^$'`. |
| 9.44 | `TestClientMiscTestSuite` + `TestClientDataConverterTestSuite` | ✅ **CLEAN — 26 pass / 0 fail / 3 corpus-native skips** (2× consecutive fresh-process runs) | 2026-07-23 | Ground truth from `common/dynamicconfig/constants.go`, `service/history/api/respondworkflowtaskcompleted/workflow_size_checker.go`, `service/history/api/command_attr_validator.go`, `service/history/api/recordworkflowtaskstarted/api.go`, `service/history/workflow/workflow_task_state_machine.go`, `service/history/workflow/mutable_state_impl.go`, `service/history/transfer_queue_active_task_executor.go`, `service/matching/matching_engine.go`, and `service/worker/batcher/activities.go @ v1.31.0` established five distinct fixes. WFT completion now receives live, explicit limits for pending child workflows, activities, external signals, and cancellation requests; the pure stateless kernel checks provisional transition-builder state, rejects atomically at the exact boundary, and authors the four exact failure causes through the existing runtime invalid-command seam. ScheduleActivity timeout and task-queue normalization moved from the edge into the pure kernel so history, state, dispatch, and timeout tracking share one deterministic decision. Sticky affinity no longer expires from elapsed wall time or mutating storage reads: storage derives both destinations, the volatile broker uses recent/active poller observations for immediate normal fallback, and affinity clears only when a fallback task actually starts on another queue; query deadlines are freshly derived at enqueue. Successful WFT completions durably evolve the current run's first-observation auto-reset points on both hot apply and replay, Describe exposes every field in order, and batch reset resolves the requested current-run build point rather than substituting the current build or first WFT; cross-run point rollover remains separately classified. Retry-policy activity starts remain transient until a terminal result materializes start then result, while retryable failures persist neither event. The Shape-2 harness forwards only the four suite-global limit keys and supplies one read-only `GetWorkflowExecution` view through `DescribeMutableState`; no corpus test body changed. Ten ≥100-case properties, fixed regressions, focused crate suites, and both corpus runs pass. The three `TestClientDataConverterTestSuite` methods remain skips authored by the upstream v1.31.0 corpus, with no Tokeira skip-registry entries. |
| 9.43 | `TestHttpApiTestSuite` | ✅ **CLEAN — 11 pass / 0 fail / 0 skip** (2× consecutive fresh-process runs) | 2026-07-22 | Tokeira now serves the descriptor-annotated WorkflowService and OperatorService HTTP/JSON surface on the existing listener and invokes the ordinary Tonic router exactly once in-process—no copied Temporal frontend topology and no kernel, runtime, storage, history, or projection change. The immutable descriptor catalog covers primary/additional bindings, legacy path-template matching, body→path→query precedence, form POST→GET fallback, and the observed wrong-method `UNIMPLEMENTED`/HTTP 501 behavior. Descriptor-aware ProtoJSON implements all four compact/pretty and payload-shorthand modes, canonical/ineligible fallback, typed status details, and the 4 MiB consumed-body limit. Host globs and header forwarding include live conformance delivery of `frontend.httpAllowedHosts`; HTTP metadata reaches the existing auth stack, with the Shape-2 callback preserving the corpus's exact authorization closure. Official API v1.62.11 OpenAPI v2/v3 assets are vendored with provenance and served as immutable prefix routes. Genuine admitted-call metrics are mapped through the harness without fabricated samples. The obsolete `/api/v1/{service}/{method}` placeholder is removed. A conformance-only post-signal scheduling grace recreates the opportunity supplied by v1.31.0's separate HTTP listener/service boundary before the corpus's immediate non-long-poll close-history read; it runs only after authoritative signal success and does not affect production or change history semantics. Eleven ≥100-case properties, body/layer/cancellation tests, OpenAPI route closure, focused Shape-2 seam tests, and both final corpus runs pass. |
| 8.42 | `TestWorkerRegistryTestSuite` | ✅ **CLEAN — 7 pass / 0 fail / 0 skip** (2× consecutive fresh-process runs) | 2026-07-21 | Baseline was 0 pass / 7 fail because `DescribeWorker` and `ListWorkers` were deferred and the Nexus poll translator discarded its piggyback heartbeat batch. Ground truth from `service/frontend/workflow_handler.go`, `service/matching/handler.go`, `service/matching/workers/registry_impl.go`, `service/matching/workers/worker_query_engine.go`, and `common/dynamicconfig/constants.go @ v1.31.0` established a volatile per-process registry: lossless latest heartbeat per namespace/key, five-minute TTL, immediate shutdown-status removal, dual full/summary list fields, worker-query filtering, and key-cursor pagination. Tokeira now preserves the complete heartbeat protobuf as an edge-owned opaque image while runtime retains compact liveness/query fields; no Temporal proto enters runtime. All four registry RPCs pass the authorization seam with v1.31.0 namespace/read-write classification. `DescribeWorker` and `ListWorkers` read the live store, return complete counters/nested data, evaluate the bounded query grammar without a SQL dependency, and paginate compatibly. Nexus-piggybacked observations are inserted after namespace admission and before blocking; failure is best effort. Compatibility ownership moves inventory from unsupported worker configuration to implemented worker heartbeats. The suite-level `WorkerHeartbeatsEnabled=true` override is intentionally unwired because v1.31.0 and Tokeira both pin the stock default true. Lossless/property, shutdown, query, pagination, Nexus-failure, surface-audit, and focused crate tests pass. No kernel, storage, projection, dependency, or Temporal-fork change. |
| 8.41 | `TestVersioning3FunctionalSuite` | ✅ **CLEAN — 319 pass / 0 fail / 4 corpus-native skips, plus 33 exact internal-topology exclusions** (2× consecutive fresh-process runs) | 2026-07-21 | Deployment routing now follows v1.31.0's observable V3 contract across target-change notification, Continue-as-New, retry and child inheritance, pinned/AutoUpgrade transitions, revision no-bounce rules, speculative/transient WFTs, activity-triggered transitions, and stale sticky migration. The kernel remains a stateless pure transition evaluator: runtime resolves mutable routing/policy/membership operands, kernel persists only deterministic per-run state/events and emits declarative dispatch effects, and replay reproduces the decision without registry or I/O access. A transition that finds an unstarted non-speculative WFT fences its disposable offer and emits a history-free replacement; speculative tasks remain timeout-driven. Sticky envelopes with missing coordinates hydrate from committed run state before comparison, and stale targets move to the normal physical Version queue. Poll admission rejects an empty sticky `normal_name` with v1.31.0's exact error. Drained-query liveness is recorded before durable Version registration and retained per physical Deployment Version, so one SDK identity polling v1 and v2 cannot overwrite v1's still-live history; absent-poller blackhole protection remains green. Public history preserves inherited Version information, target-change notification, and unknown Continue-as-New behavior values. The fork supplies scoped read-only `GetMutableState` and task-queue user-data projections through public Tokeira APIs; the 33 excluded methods directly mutate or assert Temporal's private matching topology and carry exact cited registry reasons. Properties 22–25 and focused kernel/runtime/edge tests are green. |
| 8.40 | `TestDeploymentVersionSuite` | ✅ **CLEAN — 64 active methods / 0 fail / 1 exact classified exclusion** (2× consecutive fresh-process runs) | 2026-07-20 | Workflow-option updates now carry unresolved implied-pinned intent into the pure lane-ordered kernel transition, resolve it from authoritative run state, persist only concrete overrides, and treat equivalent writes as event-free no-ops. Direct, batch, reset, start, and signal-with-start paths share explicit membership validation, committed-state responses, and post-commit best-effort Version reactivation. The runtime owns shared one-second membership and ten-second reactivation caches, pinned physical routing, and late-poller backlog re-keying; the kernel remains limited to deterministic serializable state, replay, and transition decisions. Drainage now reaches the authoritative open-pinned-workflow read through the history-notifying repository wrapper. Missing historical task queues reject Current/Ramping changes only while still owned and carrying durable backlog or recent add pressure, with v1.31.0's exact errors; idle or moved queues pass. The Temporal fork projects the suite's private task-queue user-data read from public Worker Deployment APIs. `TestForceCAN_WithOverrideState` is the sole exact exclusion because it requires the unavailable in-process override-state hook. Workflow-option Properties 4/5 and Worker Deployment Properties 20/21 run at least 100 cases, including concurrent reactivation deduplication; formatting, workspace lint, test-target lint, touched-crate tests, focused harness tests, and both corpus passes are green. |
| 8.39 | `TestWorkerDeploymentSuite` | ✅ **CLEAN — 53 active leaves / 0 fail / 3 corpus-native skips** (57 passing outcomes including nested cases and parent; 2× consecutive fresh-process runs) | 2026-07-19 | Worker Deployment poll admission now durably registers the Deployment, Version, and task-queue family before blocking and propagates admission rejection to the poller. Configured limits match v1.31.0: 100 Versions per Deployment and 100 distinct task-queue family names per Version; a second task type does not consume another family slot. At the Version cap, explicit create and automatic poll/routing creation atomically evict the oldest normally deletable Version, bypassing manager ownership only for this server maintenance, or return the exact `RESOURCE_EXHAUSTED` reason without mutation. Drainage uses the 3-minute grace/refresh contract through live conformance overrides and architecture-preserving lazy runtime recomputation with durable CAS results and fresh grace after rollback. Cancellation-aware runtime poll guards remove deletion liveness on worker shutdown, while `DescribeTaskQueue` retains bounded diagnostic history and aggregates Tokeira's physical Deployment-Version queues into the public family view. The Shape-2 cluster seam now delivers suite-level static dynamic-config overrides that were previously bypassed. Four obsolete registry skips were removed; the three remaining skips are authored by the v1.31.0 corpus itself. Runtime Property 19 runs 100 cases; focused runtime/edge/conformance tests pass. No kernel change. |
| 7.38 | `TestNexusApiTestSuiteWithTemporalFailures` + `TestNexusApiTestSuiteWithLegacyErrorPaths` | ✅ **CLEAN — 46 active leaves / 0 fail / 2 exact deprecated-versioning exclusions** (39 passing outcomes including parents per suite; 2× consecutive fresh-process runs for each mode) | 2026-07-18 | Both v1.31.0 Nexus failure representations now pass the complete Start/Cancel dispatch, claims, timeout, operation-error, handler-error, and telemetry matrix. The edge strips the caller-only `Temporal-Nexus-Failure-Support` capability before worker dispatch, maps claim-conversion failure to Nexus `UNAUTHENTICATED` independently of authorization denial, and emits normal `service_requests` alongside Nexus terminal/latency telemetry. Worker task admission metrics carry v1.31.0's namespace, operation, client-name, and internal-request dimensions for Poll and both Respond RPCs. The conformance-only authorization bridge forwards bearer/extra auth material through the corpus's exact `SetOnGetClaims` and `SetOnAuthorize` closures; the metrics bridge aliases genuine Tokeira counters and histogram observation counts without fabricating samples. Shape-2 dedicated clusters share Tokeira's correctly global endpoint catalog, so `TestEnv` cleanup removes only endpoints targeting that leaf's generated namespace before the pooled slot is reused, restoring the corpus topology's catalog isolation without weakening production uniqueness or editing a corpus test. The two namespace/task-queue Worker Versioning V1 enabled-path leaves are excluded by exact name under the resolved stock-default V1/V2 decision. No kernel change. Focused edge and harness tests pass. |
| 7.37 | `TestNexusWorkflowTestSuite` | ✅ **CLEAN — 19 active leaves / 0 fail / 0 skips** (23 passing outcomes including parents; 2× consecutive fresh-process runs) | 2026-07-18 | Nexus cancellation now has a durable child lifecycle attached to each authoritative pending operation. A pre-start request remains undispatched until the handler's `Started` outcome supplies its token; successful delivery emits `NexusOperationCancelRequestCompleted`, non-retryable delivery emits `NexusOperationCancelRequestFailed`, and retryable delivery backs off without a history event or workflow task before the runtime scanner re-dispatches it. Describe exposes the cancellation state/attempt/failure/deadline fields and orders pending operations by immutable scheduled-event ID for stable output. The parent Nexus operation remains pending after cancellation acknowledgement, matching v1.31.0. External HTTP response admission enforces both the 2 MiB raw-body cap and the decoded public Payload protobuf-size cap, and terminal call errors retain the v1.31.0 outer Nexus-operation failure chain. Kernel work is limited to deterministic serializable state, event emission, replay, fencing, and derived dispatch; HTTP, retry classification/backoff, clocks, and scanning remain runtime-owned. Focused kernel/runtime/edge tests and clippy pass. |
| 7.36 | `TestNexusAPIValidationTestSuite` | ✅ **CLEAN — 16 active leaves / 0 fail / 0 skips** (23 passing outcomes including parents; 2× consecutive fresh-process runs) | 2026-07-17 | Tokeira now serves caller-facing Nexus Start/Cancel HTTP routes on the existing tonic listener and dispatches through the disposable runtime Nexus broker to ordinary SDK Nexus workers. Both namespace/task-queue and endpoint routes implement v1.31.0 preprocessing, percent decoding, 2 MiB payload admission, timeout propagation, operation-token precedence, exact worker response/failure serialization, and terminal/preprocess telemetry. The worker transport uses the exact v1.31.0 protobuf Nexus task token, validates namespace identity and response/failure shapes before atomically consuming broker correlation, and resolves HTTP waiters without placing delivery correctness in the kernel. Authorization runs after target resolution and before payload conversion/publication; production uses the shared auth foundation, while a conformance-only namespace-scoped callback bridge preserves each corpus `SetOnAuthorize` closure without adding a production callback surface. Principal attribution, stable workflow/activity token namespace envelopes, and cross-namespace WFT-command reauthorization were completed as shared auth-foundation work; the pure kernel gained only deterministic per-event principal stamps, persisted atomically as a history sidecar and projected through public history field 303. The first harness attempt exposed only a Shape-2 observation gap: Tokeira already emitted the exact `namespace_not_found` counter, but the shared-process scraper discarded it because a deliberately missing namespace cannot belong to a registered namespace set. The fork now serializes capture windows and admits only that terminal outcome through the unknown-namespace filter, recreating Temporal's per-cluster metric-registry isolation without fabricating samples or editing corpus tests. Native auth (21), edge/conformance (438), runtime, kernel, storage+DSQL, and focused harness tests pass. |
| 7.35 | `TestNexusEndpointsFunctionalSuite` | ✅ **CLEAN — 42 active leaves / 0 fail / 0 skips** (54 passing outcomes including parents; 2× consecutive fresh-process runs) | 2026-07-16 | Tokeira's existing OperatorService endpoint implementation passed every public create/get/update/delete/list validation and fidelity assertion without a product change. The initial 15-pass/13-fail baseline was a Shape-2 harness defect: the corpus's shared setup and Matching/Common leaves call Temporal's private MatchingService and persistence-manager views, while the out-of-process shim embedded a nil generated client. The Temporal fork now translates those views onto the real Tokeira OperatorService registry, preserving v1.31.0's ID-ordered internal pagination, endpoint/table versions, long-poll wakeup, error typing, persistence ordering, namespace-ID representation, and non-nil internal HLC projection. Only the private matching-owner table-version/notification metadata is kept in the harness because the public API has no representation for it; all endpoint records and mutations remain authoritative in Tokeira. Cluster-global endpoints retain their registered namespace IDs across per-suite cluster teardown through a process-scoped identity catalog, without widening per-cluster metric scope. Focused Shape-2 adapter tests pass. No Tokeira code, kernel, skip-registry, or corpus-test change. |
| 6.34 | `TestActivityApiResetClientTestSuite` + `TestActivityAPIUpdateClientTestSuite` + `TestActivityApiBatchUpdateOptionsClientTestSuite` + `TestActivityApiRulesClientTestSuite` | ✅ **CLEAN — 15 active leaves / 0 fail / 0 skips** (2× fresh-process runs for rules, direct update, and batch update; repeated clean reset runs) | 2026-07-16 | The workflow-scoped activity-control RPCs are restored to the v1.31.0 surface: their API comments announce future deprecation but the target release has neither formal deprecation markers nor replacement RPCs. Pause is idempotent, fences only scheduled work, and lets a running attempt finish; paused retryable failure advances the attempt, clears started fields, and parks without dispatch. Reset applies attempt-one, keep-paused, jitter, restore-original options, deferred heartbeat clearing, and reset/pause heartbeat flags; unpause is a no-op when already unpaused and bumps the stamp for every actual unpause. The shared attempt validator remains intact, matching `IsActivityTaskNotFoundForToken` in v1.31.0. UpdateActivityOptions supports id/type/all selectors, retry-policy field masks, original schedule-event restoration, and both direct and batch execution. Workflow Rules now use a durable namespace registry with v1.31.0 duplicate, capacity, expiration-retention, and CRUD-gate semantics. Ordinary, eager, retry-preparation, and retry-timer paths all evaluate current rules independently of the CRUD gate and persist rule-derived pause provenance before dispatch; a deterministic parked-poll regression and six fresh-process `PrePause` runs cover the former admission race. Kernel work remains limited to deterministic activity state/dispatch decisions; rule registry/evaluation, heartbeat, and retry preparation stay storage/runtime-owned. Runner totals: reset 6/6, direct update 5/5, batch update 3/3, rules 5/5. No Temporal-fork change. |
| 6.33 | `TestStandaloneActivityTestSuite` | ✅ **CLEAN — 174 pass / 0 fail / 0 skips** (2× consecutive fresh-process final-binary runs) | 2026-07-14 | Baseline with standalone activities disabled was 2 pass / 125 fail; after delivering the suite's live overrides, the actionable baseline was 125 pass / 39 fail. The conformance bridge now applies v1.31.0's standalone-activity enablement, long-poll timeout/buffer, and visibility page-size settings at runtime, and conformance builds keep the timer sweeper available so an enablement override received after process start is effective. Standalone polling now waits on committed dispatch notifications for delayed and timer-created retries, while never-seen queues still fall through to ordinary workflow activity polling. Activity state preserves completion/failure worker identity, cancellation requester identity and details, termination identity, prior-attempt completion time, current retry interval, and current-attempt scheduled time; Describe returns normalized retry-policy and timeout defaults, explicit empty search attributes, and omits `last_failure` for canceled/terminated outcomes. Caller search attributes flow into activity visibility, list results retain them, count supports `GROUP BY ExecutionStatus` with public payload encoding, page sizes default/cap at 1,000, and invalid visibility expressions map to `INVALID_ARGUMENT`. All behavior was verified against the standalone CHASM implementation in Temporal v1.31.0. No kernel or Temporal-fork change: the implementation remains in the CHASM activity component/state machine, edge, projection, conformance bridge, and conformance-only boot wiring. Focused CHASM, edge, projection, and conformance tests pass. |
| 5.32 | `TestCallbacksSuiteHSM` + `TestCallbacksSuiteCHASM` | ✅ **CLEAN — 11 active HSM leaf passes / 0 fail / 1 classified top-level CHASM exclusion** (2× consecutive fresh-process final-binary HSM runs) | 2026-07-13 | Combined baseline: 6 pass / 22 fail. The edge now applies v1.31.0 callback count, URL, header, scheme/host, wildcard-address, and `AllowInsecure` validation with exact messages; the conformance bridge carries the three scalar overrides plus structured `allowedAddresses` JSON. Continuation closes no longer fire callbacks: continue-as-new and retry-in-progress failure/timeout preserve `Standby` callbacks for the successor, while chain-terminal closes still dispatch. Callback attempts now record a one-based count and completion timestamp for every result, clear prior failure on success, decode Nexus handler errors to v1.31.0's public error text, and expose the complete state through Describe. The retry scanner dispatches due callbacks independently, eliminating a deterministic multi-callback head-of-line deadlock. Kernel involvement is limited to the authoritative continuation-vs-terminal eligibility clamp and attempt-state transition; all HTTP/config work remains runtime/edge. CHASM is an exact registry exclusion because the suite requires the stock server's internal CHASM feature mode, already outside the campaign scope. Each final HSM runner tally was 14 pass / 0 fail / 0 skip including parents. Production external-callback allow-list configuration remains OPEN: feature-off Tokeira retains accept-after-shape-validation rather than v1.31.0's empty-list rejection default. |
| 5.31 | `TestLinksTestSuite` | ✅ **CLEAN — 4 active leaf passes / 0 fail / 0 skips** (2× consecutive fresh-process final-binary runs) | 2026-07-13 | Baseline 2/4: SignalWorkflow and SignalWithStart already preserved links, while RequestCancel and Terminate validated their links with the shared v1.31.0 count/size/type/identity rules but dropped them before the transition. The corrected `grpc-edge-transport` spec now requires lifecycle-link preservation. The edge maps validated ordered links into the existing internal model, the pure kernel carries them as deterministic request/event data on `WorkflowExecutionCancelRequested` and `WorkflowExecutionTerminated`, and the history serializer emits them on the outer `HistoryEvent.links` field. Internal lifecycle producers explicitly author empty link lists. Generated edge round-trip and kernel event-pass-through properties plus a 100-case serializer property prove ordered equality for both operations. The kernel change is data-shape only: no I/O, async, storage, metrics, or new transition decision. Each final runner tally was 5 pass / 0 fail / 0 skip including the suite parent. No Temporal-fork change. |
| 5.30 | `TestScheduleV1` | ✅ **CLEAN — 15 active public leaves pass / 0 fail / 4 classified internal-implementation exclusions** (2× consecutive fresh-process final-binary runs) | 2026-07-13 | The V1 schedule surface now preserves workflow input, headers, user metadata, schedule-owned search attributes, terminal action status, last successful completion result, and continued failure; schedule firings are ordinary workflow starts rather than Workflow Cron executions. Calendar/cron normalization now matches v1.31.0 for stepped singleton ranges, omitted zero interval phase, comments, `(start, end]` matching-time bounds, first-evaluation creation bounds, and the ten-entry ListSchedules spec projection limit. Update preserves absent search attributes, rejects memo replacement with the v1.31.0 precondition, duplicate creation carries `WorkflowExecutionAlreadyStartedFailure`, internal per-namespace task queues are rejected, schedule visibility filters support conjunctions/disjunctions and `TemporalSchedulePaused`, and scheduled starts observe the conformance-wired `worker.schedulerNamespaceStartWorkflowRPS` per-namespace rate. The Temporal fork also binds proto assertions to each Shape-2 subtest and unwraps the runner's single namespace-constrained dynamic-config value without inventing multi-constraint precedence. Classified exclusions: `TestRefresh` and `TestNextTimeCache` inspect Temporal's private scheduler-workflow history/state; the two CHASM-sentinel leaves require its unavailable in-process `SchedulerClient` and migration internals. Because the runner applies these through `go test -skip`, its final tally is 18 pass / 0 fail / 0 emitted skip (suite parent + 15 active leaves + two nested internal-task-queue cases); focused registry tests prove all four exclusions and keep public siblings active. Tokeira's native schedule store, carried completion/failure state, and rate limiter remain process-local; restart durability is still open. No kernel change. |
| 4.29 | `TestTaskQueueSuite` + `TestPollerScalingFunctionalSuite` | ✅ **CLEAN — 9 active leaf passes / 0 fail / 4 classified skips + 4 corpus-native flaky skips** (2× consecutive final-binary runs) | 2026-07-13 | Baseline exposed matching-plane fidelity gaps, all resolved without kernel changes. Poll responses now project `PollerScalingDecision(+1)` from remaining runnable workflow, activity, and Nexus pressure; `DescribeTaskQueue` and deployment-version Describe expose live runnable backlog count/oldest age; current and 100%-ramping Worker Deployment versions absorb and report pre-promotion unversioned workflow/activity backlog. Activity polls retain deployment/build, worker-instance, and worker-advertised rate metadata and auto-register activity queue membership. Queue-wide API rate limits take precedence over worker limits, including zero-rate suppression; config Update/Describe preserves partial updates, explicit-unset audit metadata, fairness maps, and the v1.31.0 maximum-override admission limit. The conformance-wired, stock-default-off `frontend.enableCancelWorkerPollsOnShutdown` policy wakes and fences workflow/activity long polls and eagerly removes poller history. Active task-queue leaves cover shutdown, zero/update rate limits, and config update/unset; all four poller-scaling leaves pass, including current/ramping deployment backlog. Four upstream methods self-skip as flaky. Classified skips: three dispatch-latency metric leaves require Temporal's unavailable in-process matching client, capture handler, and forward-delay hook; `TestTaskQueueRateLimit` compares internal old/new matcher and partition-forwarding modes that Tokeira does not expose, while its public rate-limit siblings remain active. The scaling mechanism deliberately uses live remaining backlog rather than v1.31.0's sampled add/dispatch-rate window, producing the exercised positive recommendation but not claiming the full untested idle `-1` heuristic. Final combined runner tally on each run: 11 pass / 0 fail / 8 skip including suite parents. Focused broker promotion/backlog/shutdown, poller registry, config metadata, proto projection, and override-registry tests pass. |
| 4.28 | `TestNamespaceSuite` + `TestNamespaceInterceptorTestSuite` | ✅ **CLEAN — 7 active leaf passes / 0 fail / 2 classified skips** (2× consecutive final-binary runs) | 2026-07-12 | Baseline exposed two real public gaps. `RespondWorkflowTaskCompleted` trusted the token but did not enforce equality with a non-empty request namespace; it now resolves the token's authoritative run namespace and returns v1.31.0's exact `INVALID_ARGUMENT` before consuming query results or invoking runtime mutation. `OperatorService.DeleteNamespace` was stubbed, and its owning spec incorrectly required rejection when open workflows existed and prohibited history deletion. Ground truth from `service/frontend/operator_handler.go`, `service/worker/deletenamespace/{workflow.go,activities.go,reclaimresources/workflow.go}`, `common/rpc/interceptor/namespace_validator.go`, and `common/dynamicconfig/constants.go @ v1.31.0` corrected the spec: validate name xor ID, mark `DELETED`, atomically rename to `<name>-deleted-<id-prefix>` while preserving stable ID, return immediately, asynchronously enumerate authoritative namespace runs and delete each through the existing fenced runtime/storage path, apply visibility tombstones, then remove the namespace tombstone after the explicit delay or zero default. `DescribeNamespace` by ID observes the interim deleted state; final absence now carries `NamespaceNotFoundFailure`, so SDKs reconstruct typed `serviceerror.NamespaceNotFound`. The five public deletion leaves, empty upstream TODO leaf, and interceptor leaf pass. Classified skips: `WithMissingWorkflows` directly reaches the unavailable in-process `ExecutionManager`; `Protected` requires a per-test random `[]string` dynamic-config override not representable by the conformance override protocol. Required selector/name/delay/reclaim/token properties and focused wire tests pass. No kernel change. Final combined runner tally: 9 pass / 0 fail / 2 skip including suite parents. |
| 4.27 | `TestUserMetadataSuite` | ✅ **CLEAN — 3 active leaf passes / 0 fail / 0 skips** (2× consecutive final-binary runs) | 2026-07-12 | Baseline 0/3: Start, Signal-with-Start, and ExecuteMultiOperation all successfully created the workflow, but `DescribeWorkflowExecution.execution_config.user_metadata` was always nil. The authoritative state and start history already retained and replayed both payloads; only the read model hardcoded `None`, and its DTO incorrectly modeled metadata as one payload instead of the API's `{summary, details}` pair. Ground truth: v1.31.0 fetches the start event and copies `startEvent.UserMetadata` into `WorkflowExecutionConfig` (`service/history/api/describeworkflow/api.go:98-110`). The store-backed resolver now maps committed start metadata into the Describe DTO, and gRPC translation preserves both payloads and their metadata/data bytes. Focused payload-exact translation test passes. Both final runs reported runner tally 5 pass / 0 fail / 0 skip including the parallel-suite parents; the MultiOperation leaf remained active and passed without a harness exclusion. No kernel or Temporal-fork change. |
| 4.26 | `TestWorkflowAliasSearchAttributeTestSuite` | ✅ **CLEAN — 2 active leaf passes / 0 fail / 0 skips** (2× consecutive final-binary runs) | 2026-07-12 | Baseline aborted before assertions because Shape-2 exposed no matching client and the suite's `CheckTaskQueueVersionMembership` helper dereferenced nil. The Temporal fork now adapts that one matching read to the equivalent public `DescribeWorkerDeploymentVersion` projection, resolving the namespace ID learned during registration and checking deployment-version task-queue membership without manufacturing readiness. Once exposed, both leaves failed because the edge dropped the modern `VersioningOverride.override` oneof and visibility lacked Worker Deployment system attributes. The edge now preserves modern pinned/auto-upgrade overrides (while retaining deprecated-field reads); storage derives `TemporalWorkflowVersioningBehavior`, `TemporalWorkerDeployment`, and `TemporalWorkerDeploymentVersion` from committed versioning state into projection snapshots without mutating history/user search attributes. The query compiler mirrors v1.31.0's exact-name-first then add/remove-`Temporal` prefix fallback (`common/persistence/visibility/store/query/resolve.go`), so an explicitly registered custom `WorkflowVersioningBehavior` wins while the canonical system attribute remains independently queryable. Focused edge, storage, projection, and harness tests pass; both final runs reported runner tally 3 pass / 0 fail / 0 skip including the suite parent. No kernel change. |
| 4.25 | `TestWorkflowMemoTestSuite` | ✅ **CLEAN — 2 active leaf passes / 0 fail / 0 skips** (single clean baseline run; green with no fix) | 2026-07-12 | Green at baseline — no engine or harness change. Both `StartWorkflowExecution` and `SignalWithStartWorkflowExecution` preserve the `Memo{Info: "memo-value"}` payload through every client-observable representation the suite checks: the initial `WorkflowExecutionStarted` history event; open visibility; `DescribeWorkflowExecution` while running; exact completed history (`Started → [Signaled] → WFT Scheduled/Started/Completed → WorkflowExecutionCompleted`); Describe immediately after close but before visibility convergence; closed visibility; and Describe after close visibility settles. Runner tally was 3 pass / 0 fail / 0 skip including the suite parent. Marked from one clean baseline, following Tier 4.23's no-fix convention rather than spending the fix-driven 2× stress budget. |
| 4.24 | `TestAdvancedVisibilitySuite` + `TestAdvancedVisibilitySuiteLegacy` | ✅ **CLEAN — 40 active leaf passes / 0 fail / 2 corpus-native skips + 22 classified V1/V2 enabled-path exclusions** (2× consecutive isolated final-binary runs per converter mode) | 2026-07-12 | Baseline across both converters was 5 pass / 57 fail / 2 corpus-native Elasticsearch-only skips. Landed the v1.31.0 visibility contract in four slices. **Durable workflow-property semantics:** workflow-task memo and search-attribute upserts now author replayable kernel events with null-as-delete patches; invalid search-attribute keys fail the WFT with `BadSearchAttributes`. **Projection/query fidelity:** typed SA normalization, complete List payloads, parent/root lineage, nanosecond time fields, case-insensitive Boolean grouping/parentheses, Text token-set equality, `IS NULL`, `NOT STARTS_WITH`, exact-full-page tokens, Count `GROUP BY ExecutionStatus`, and schedule-list filtering before pagination. `ScheduleId` follows v1.31.0's synthetic-field rule: a custom SA of the same name takes precedence, otherwise the real schedule ID is used (`common/searchattribute/sadefs/constants.go @ v1.31.0`). **History-derived system attributes:** the pure kernel records typed `WorkerVersionStamp` + `binary_checksum`, reconstructs ordered per-run `BuildIds` and the most-recent stamp on replay, and accumulates external-payload count/declared bytes from committed events; storage projects those server-managed values without placing them in inheritable user SAs. The retained `Test_BuildIdIndexedOnCompletion_UnversionedWorker` passes accumulation, continue-as-new isolation, close-time indexing, and visibility queries. **Worker Versioning boundary:** 11 methods per mode require the non-default deprecated V1/V2 enabled path and are classified by the fork registry under `.kiro/specs/worker-deployments/reference/v1-v2-conformance-decision.md`; the stock-default five-RPC rejection behavior is implemented and tested separately. Each final run reported runner tally 21 pass / 0 fail / 1 skip (parent included); `TestListWorkflow_OrderBy` is the sole corpus-native skip. Focused replay/external-payload/schedule tests pass; kernel, runtime, storage, projection, edge, and tokeirad affected test targets all compile; formatting and both-repository diff checks are clean. |
| 4.23 | `TestWorkflowVisibilityTestSuite` | ✅ **CLEAN — 2 pass / 0 fail / 0 skips** (single clean baseline run; green with no fix) | 2026-07-12 | Green at baseline — no engine change. The suite has one leaf, `TestVisibility`: it starts two workflows, completes one via a real `TaskPoller`, long-polls `GetWorkflowExecutionHistory` for the close event, then asserts the legacy standard-visibility list surface — `ListClosedWorkflowExecutions` (StartTime + WorkflowType filter → 1 closed, `historyLength == 5`, single page) and `ListOpenWorkflowExecutions` (→ 1 open, single page). Basic list path only; the rich query surface (ORDER BY / BETWEEN / keyword IN / search attributes / Count) is Tier 4.24 `AdvancedVisibilitySuite` (32 methods × 2 toggles). The `TestVisibilityTestSuite` named in `functional-test-order.md` is a `tests/xdc/` cross-cluster suite, out of public scope. Marked from a single clean baseline (zero baseline failures, nothing to fix) rather than the 2× stress the fix-driven tiers cite. |
| 3.22 | `TestWFTFailureReportedProblemsTestSuite` | ✅ **CLEAN — 5 pass / 0 fail / 0 skips** (2× consecutive final-binary runs, post-adversarial-review) | 2026-07-12 | Baseline 1/4 (only `_NotClearedBySignals` passed). Diagnosis (`docs/findings-tier322-reported-problems.md`, retired to git history): the Go SDK (v1.41.1) sends `RespondWorkflowTaskFailed` only when `PollWorkflowTaskQueueResponse.attempt == 1` (`internal_task_pollers.go:558-563` — "subsequent failure on the same workflow task will timeout"); attempts ≥2 are dropped client-side and retried by the server's start-to-close timer, and the runtime-local problem tracker counted only the RPC path, so the counter stalled at 1 < threshold 2. Landed: **`AttemptsSinceLastSuccess` moved into kernel state** (`WorkflowState.workflow_task_attempts_since_last_success` + `last_workflow_task_problem`, the persisted `LastWorkflowTaskFailure` analog), advancing under exactly v1.31.0's `failWorkflowTask` guard (`workflow_task_state_machine.go:1010-1027`): non-sticky failures AND non-sticky StartToClose timeouts count (transient included), ScheduleToStart never counts, completion clears both; the problem identity is written only when the event persists (non-transient), so a threshold crossed by a transient timeout still reports the attempt-1 cause — the exact contract the suite asserts. Includes the **sticky raise S4** (deferred since Tier 1.4): a failure with the real sticky queue set clears the affinity and retries a fresh attempt-1 normal-queue task with NO attempt/counter increment (`:1012-1015`), and the poll-side hint no longer clobbers real sticky at start. Buffered-limit/terminate force-closes count through the same funnel; replay arms mirror; server-decided WFT failures (invalid-command/bad-update/reset) now count by construction since they submit `Command::WorkflowTaskFailed`. Describe derives from committed state (the volatile-tracker OPEN is retired) and renders the `TimedOut` category pair (`mutable_state_impl.go:6486-6491`). Regression: `TestStickyTqTestSuite` 3/0/0, `TestTransientTaskSuite` 3/0/1 (pre-existing skip), `TestMaxBufferedEventSuite` 2/0/1 (pre-existing skip), `cargo test --workspace` green (126 suites), 9 new kernel counting tests. Override delivery positively confirmed: control listener bound, no bridge fallback for the threshold key, and leaf 4's 15s phase-2 window is unreachable at the pinned default 5. |
| 3.21 | `TestDescribeTestSuite` | ✅ **CLEAN — 2 pass / 0 fail / 0 skips** (2× consecutive final-binary runs) | 2026-07-10 | Baseline 0/2. **DescribeWorkflowExecution:** `history_size_bytes` was always zero. Temporal v1.31.0 reads the field from its denormalized execution-statistics history-size counter (`service/history/api/describeworkflow/api.go:126`); Tokeira instead derives it on read from the full committed authoritative history using the same protobuf representation returned by `GetWorkflowExecutionHistory`. This deliberately avoids a drift-prone counter while keeping the value client-meaningful; the corpus asserts positivity, not byte-for-byte equality. The existing history read is shared with external-payload statistics. **DescribeTaskQueue:** the edge registry modeled only currently open long polls and removed each identity when its RAII guard dropped, but v1.31.0's matching layer retains a deduplicated identity history for the five-minute `PollerHistoryTTL` (`service/matching/poller_history.go`, `service/matching/physical_task_queue_manager.go`, and `common/dynamicconfig/constants.go:479`). Poll admission and non-cancelled completion now refresh `last_access_time`; a poll ended by client/worker cancellation does not refresh, mirroring v1.31.0's `ctx.Err() != context.Canceled` guard (`service/matching/task_queue_partition_manager.go:617-621`). Describe prunes at the TTL boundary. Tokeira additionally sorts pollers by identity for stable output; v1.31.0 iterates its poller cache in unspecified order and guarantees none. Tokeira does not yet expose v1.31.0's matching-internal `CancelOutstandingWorkerPolls` shutdown signal (`service/matching/matching_engine.go:1194-1206`), so a stopped worker may remain visible for at most five minutes; the Kiro requirements/design/tasks explicitly record this bounded diagnostic deviation. No kernel change. Focused evidence: edge all-target check; four deterministic poller tests plus the 100-case TTL/dedup property; Describe projection and public-history serialization properties; tokeirad all-target check; and the production-shaped start/Describe/reflection roundtrip. |
| 3.20 | `TestSizeLimitFunctionalSuite` | ✅ **CLEAN — 0 pass / 0 fail / 4 classified skips** (2×) | 2026-07-10 | Baseline 0/4: every leaf expected a test-only dynamic-config threshold that the out-of-process Shape-2 seam cannot deliver. Ground truth from `tests/sizelimit_test.go` and `common/dynamicconfig/constants.go @ v1.31.0` shows the exact unreachable premises: history-count error 20 vs default `50*1024` events; blob error 1,000 bytes vs 2MiB; mutable-state error 1,100 bytes vs 8MiB; history-size error 9,000 bytes vs 50MiB. Temporal fork commit `3669a431f` classifies all four leaves individually through the cited `WithDynamicConfig` skip registry, with generated-regexp unit coverage; no corpus body was edited. No Tokeira production change was made: lowering release-pinned limits merely to trigger a test would violate the established config-as-constant convention. These classified skips describe harness reachability and do not claim that default-threshold force-termination behavior is independently proven by this suite. |
| 3.19 | `TestWorkflowDeleteExecutionSuite` | ✅ **CLEAN — 3 pass / 0 fail / 0 skips** (2× consecutive final-binary runs; accumulated Tiers 1.1–3.19: 29/29 entrypoints) | 2026-07-10 | Baseline 0/3: the visibility-only handler returned success while mutable state and history remained readable. Ground truth from `service/frontend/workflow_handler.go`, `service/frontend/validators.go`, `service/history/api/deleteworkflow/api.go`, `service/history/transfer_queue_task_executor_base.go`, `service/history/shard/context_impl.go`, and `service/history/consts/const.go @ v1.31.0` established exact validation, terminate-before-delete, identity, and purge semantics. **(1) Authoritative exact-run deletion.** Runtime now terminates an open target with reason `Delete workflow execution` and identity `history-service`, reloads it, and performs an OCC- and execution-home-epoch-fenced storage purge; closed runs skip the termination transition. In-memory and DSQL storage atomically author the deletion projection tombstone and remove mutable state, history, current pointer only when it names the target, dedupe, activity, timer, dispatch, backlog, audit, and shard rows. **(2) No retargeting.** Omitted `run_id` resolves the current pointer exactly once; deletion never falls back to an older surviving run, and OCC retry reloads only that resolved run. **(3) Monotonic derived removal.** Visibility retains a versioned non-queryable Deleted high-water row, clears search indexes, subtracts rollups, and rejects delayed older snapshots so projection replay cannot resurrect the execution; synchronous direct and batch deletion share the same runtime coordinator and tombstone operation. **(4) Wire fidelity.** Missing execution, empty workflow id, and malformed non-empty run id map to v1.31.0 INVALID_ARGUMENT messages; missing/deleted runs return NOT_FOUND. Focused suites are green: storage 68, projection 58, runtime 339, edge 292, and tokeirad 25 tests, including 100+ case Properties 5/10/11/12 and DSQL deletion guards. The accumulated regression selection used the fork's classified-skip registry and completed all 29 top-level entrypoints through Tier 3.19 with zero failures. The pure kernel is unchanged: deletion is operational cleanup and only reuses the existing Terminate command for open runs. |
| 3.18 | `TestEagerWorkflowTestSuite` | ✅ **CLEAN — 5 pass / 0 fail / 1 classified skip** (3× stress) | 2026-07-09 | Baseline 0/6: every leaf omitted `eager_workflow_task`. Ground truth from `service/history/api/startworkflow/api.go`, `service/history/historybuilder/event_factory.go`, and `service/history/api/create_workflow_util.go @ v1.31.0` showed that the API handler pre-gates the request, then run creation durably records the accepted flag while atomically authoring WES / WFT Scheduled / WFT Started. **(1) Durable agreement.** `StartRequest` now carries the runtime-owned `eager_execution_accepted` candidate; the pure kernel only clamps it false unless that same transition starts the first WFT inline. New histories use append-only `WorkflowExecutionStartedV2` because postcard enum payloads are positional; the legacy variant remains decode-only, with genuine pre-change bytes proving V1 decodes as false and V2 round-trips the bool. **(2) Correct admission and delivery.** The runtime pins v1.31.0's enabled default, applies cron/start-delay backoff before admission, identifies the request caller as the inline worker, and builds the response from committed state without requiring `PollerRegistry` liveness or publishing/reclaiming through the broker. **(3) Idempotent retry.** A repeated request id reconstructs event-3 attempt-1 only while its start-to-close deadline is live; elapsed/completed/timed-out tasks are omitted without another transition. **(4) Capability truth.** System and namespace capabilities now advertise eager workflow start, and the compatibility matrix is Implemented with corpus evidence. Skip: `TestEagerWorkflowStart_TerminateDuplicate` requires the test-only `OverrideDynamicConfig(WorkflowIdReuseMinimalInterval=0)`; that server-side override cannot be delivered to an out-of-process target over the public wire. |
| 3.17 | `TestResetWorkflowTestSuite` + `TestWorkflowResetTestSuite` + `TestWorkflowResetWithChildTestSuite` | ✅ **CLEAN — 31 pass / 0 fail / 8 classified skips** (full regression sweep of Tiers 1.1–3.16 + 3.18 unregressed: 258 pass / 0 fail / 24 classified skips; `cargo test --workspace` 118 binaries green) | 2026-07-10 | Owning spec `.kiro/specs/workflow-reset/` (committed `d7ed3358`). Baseline 0/14 active. Grounded in v1.31.0 `resetworkflow/api.go` + `ndc/workflow_resetter.go` + `mutable_state_impl.go`. **Slice 1 — reset logic (`c33e3e19` + `e7efdb8c`):** closed-run fork (CM4, no parent-close on reset), fork-point WFT failure authored on the SUCCESSOR (marker on the base is terminal; the successor's boundary failure is non-terminal + re-enqueues pending activities — the F11 "no tasks" hang), synthesize-started for a scheduled-not-started fork WFT (F5), range-based reset-point resolution (CM2), FirstRunId lineage (F3). **AdminService + CM5 (`5619b8dc`, `e79ae397`):** minimal wire-compatible `AdminService.DescribeMutableState` (self-contained proto, field numbers match the real adminservice/persistence protos), reset lineage links (`reset_run_id` base→new, `original_execution_run_id` new→origin), base/current split — the OPEN current (current-open pointer, NOT `find_latest_run`) is terminated after the fork iff distinct from the base. **Slices 2+3 — reapply engine:** the lane reads the base's post-fork history (paged — the DSQL store clamps unbounded limits) and collects reapply-eligible events per v1.31.0 `reapplyEvents`: `WorkflowExecutionOptionsUpdated` ALWAYS (even under `RESET_REAPPLY_TYPE_NONE`), `Signaled` unless excluded, `UpdateAccepted`→NEW appended postcard kind `WorkflowExecutionUpdateAdmitted` (origin REAPPLY) unless excluded, `UpdateAdmitted` passed through (repeated resets); the exclude set from `GetResetReapplyExcludeTypes` (type SIGNAL→{UPDATE}, NONE→{SIGNAL,UPDATE}, explicit excludes unioned) rides the committed `Command::Reset` — NOT the persisted `WorkflowTaskFailed` marker, whose postcard-positional shape is frozen (an in-place field addition broke persisted-history decode; caught by pre-commit multi-agent review, reverted). The kernel emits reapplied events BETWEEN the fork-point `WorkflowTaskFailed` and the fresh `WorkflowTaskScheduled`, mirroring the cold-replay state mutations (admitted-updates registry, request-id infos + workflow-scoped dedupe re-point — the re-sent attach start dedupes to the successor with no new event — callbacks/links/versioning). Buffered events flush onto the base right after its reset marker ("fails then flushes", util.go:26), which is what makes a buffered signal reapplyable (or droppable under NONE). The reset RPC is synchronous like upstream: bounded wait for the successor's `WorkflowTaskFailed(ResetWorkflow)` boundary at/after the fork point. Successor timeout windows RESTART at reset (`RefreshExpirationTimeoutTask`, mutable_state_impl.go:8417): materialization stamps `started_at`/`first_run_started_at` = now in BOTH stores — replayed prefix timestamps left the successor born-expired (`TestResetWorkflowAfterTimeout` raced its own run-timeout). Conformance-forced relaxation: update-acceptance validation no longer rejects NEGATIVE `accepted_request_sequencing_event_id` (the corpus sends `-1` as a deliberate fake; v1.31.0 records it verbatim) — the F5 owner amendment survives as the forward-reference rejection only. A reapply-read failure FAILS the reset RPC (same posture as materialization failure) instead of degrading to an empty reapply. **Slice 4 — reset-with-child:** child non-init events on the base's tail reapply onto the successor when it still holds the matching pending child (initiated inside the copied prefix) — `reapplyChildEvents` (workflow_resetter.go:1039): Started stamps the pending child (skip if already started), the five terminals + StartChildFailed retire it and deliver the resolution; Initiated is NOT reapplied (phase-2's terminate-and-start producer is commented out upstream, hence the 6 upstream-skipped leaves). A live child's later close follows the terminated parent's `reset_run_id` lineage to the successor (bounded 100 hops — recordchildworkflowcompleted/api.go:47-54): the lane's child-resolution delivery redirects when the recorded parent run is closed with a reset link. Skips: 6 upstream phase-2 child leaves + 2 OptionsUpdate matching-RPC registry class; the 2 worker-deployment-versioning leaves (nil MatchingClient SIGSEGV) are fork-skipped (`55f77ec33`). |
| 3.16 | `TestCronTestSuite` + `TestCronTestClientSuite` | ✅ **CLEAN — 5 pass / 0 fail / 0 skips** (3× stress; Tiers 1.1–3.15 re-verified unregressed — CaN/Child/Timer/Failures/Buffered 20/0/0; `cargo test --workspace` 118 binaries green) | 2026-07-08 | Baseline 2/5. The tier that overturned tokeira's cron model, grounded in v1.31.0 `respondworkflowtaskcompleted/workflow_task_completed_handler.go` (`handleCron` → `AddCompletedWorkflowEvent(…, newExecutionRunID)`) + `common/backoff/cron.go` (`GetBackoffForNextSchedule`) + `mutable_state_impl.go` (ExecutionTime = StartTime + FirstWorkflowTaskBackoff). **(1) Cron close = real outcome, not ContinueAsNew.** tokeira wrongly closed each cron run with `WorkflowExecutionContinuedAsNew` (status CONTINUED_AS_NEW). v1.31.0 closes with the run's REAL terminal event — `WorkflowExecutionCompleted`/`Failed`/`TimedOut` — carrying `NewExecutionRunId`, and the successor is a SEPARATE new run started with `CONTINUE_AS_NEW_INITIATOR_CRON_SCHEDULE`. Added `new_execution_run_id` to the `WorkflowExecutionCompleted` event; the runtime starts the cron successor as a derived post-commit effect (same posture as the retry successor — the close stands even if the successor start fails, dedup on a deterministic `cron-successor:*` request id), retiring `emit_cron_continue_as_new`. `TestCronWorkflowCompletionStates` now observes COMPLETED/FAILED/TIMED_OUT closes instead of CONTINUED_AS_NEW. **(2) `WorkflowExecutionStarted.Initiator`** threaded edge→command→WES = CRON_SCHEDULE for cron starts/successors (landed in the tier checkpoint `59f6e4c7`, with the failed-cron `LastCompletionResult` carry-forward — a failed run carries the last SUCCESS forward, independent of its own failure). **(3) Visibility `ExecutionTime`.** `ListOpenWorkflowExecutions` returned an unset `ExecutionTime` (epoch) so `ExecutionTime − StartTime` was garbage instead of the 3s cron backoff (`TestCronWorkflow`). The durable write already stored `execution_time = started_at + workflow_start_delay` (v1.31.0's StartTime + FirstWorkflowTaskBackoff); the read path dropped it — carried it through `WorkflowExecutionSummary` → `workflow_execution_info_from_summary` (was hardcoded `None`). **(4) Timeout `RetryState`.** A cron run with no retry policy that times out reports `RETRY_STATE_RETRY_POLICY_NOT_SET` (5), not `IN_PROGRESS` — the scanner's InProgress override now gates on a RETRY successor, not any successor (a cron successor is orthogonal to retry state). **(5) Run-timeout anchor = EXECUTION time.** In v1.31.0 the run does not begin (and its WES is not written) until the first-WFT backoff elapses, so the run timeout is measured from the execution time. tokeira creates the run eagerly and delays the first WFT, so the timeout entry now carries `workflow_start_delay` and anchors the run timeout on `started_at + workflow_start_delay` (threaded through the durable `WorkflowTimeoutSweepEntry` so it survives shard recovery); without it the 5s run timeout on `CompletionStates`' third run fired ~3s early. **(6) Cron backoff = `GetBackoffForNextSchedule`.** Replaced the fixed-interval `@every` backoff with Temporal's phase-anchored computation: start from the closing run's scheduled (execution) time, advance `Schedule.Next()` past `now`, round the remainder UP to whole seconds. A run that outlived one or more intervals (the timed-out third run) now lands on the schedule's phase rather than `now + interval`. The timeout successor anchors on the precise run-timeout DEADLINE (execution start + run timeout), not the ≤1s-jittery periodic scan instant, so the next fire is deterministic. OPEN (not corpus-visible): the cron successor's `WorkflowExecutionStarted` time is wall-clock for a completion successor vs the deadline for a timeout successor (both correct for their path); `first_run_started_at` (execution-timeout anchor) still omits the first run's own backoff — no cron execution-timeout leaf pins it. |
| 3.15 | `TestContinueAsNewTestSuite` | ✅ **CLEAN — 8 pass / 0 fail / 0 skips** (3× stress; Tiers 1.1–3.14 re-verified unregressed; `cargo test --workspace` green) | 2026-07-08 | Baseline 1/8. Seven leaves, grounded in v1.31.0 `command_attr_validator.go` (ValidateContinueAsNew / ValidateStartChild) + `mutable_state_impl.go` (ContinueAsNewMinBackoff) + `transfer_queue_active_task_executor.go` (parent-close terminate). **(1) CaN header** threaded edge→command→`WorkflowExecutionContinuedAsNew` event→successor `WorkflowExecutionStarted` (new `header` on the CaN command + event; the CaN event proto itself has no header — it authors the successor's). **(2) Inherited task queue / workflow type / execution timeout**: a CaN or child command that omits `task_queue` (and CaN also `workflow_type`) inherits it from the current / parent run (v1.31.0 "Inherit … from previous execution if not provided"); the edge passes empty through and the kernel defaults it. The workflow EXECUTION timeout is always inherited from the chain's first run (the CaN command has no such field) so the deadline spans the whole chain — without it `TestContinueAsNewRunExecutionTimeout` never timed out. **(3) ContinueAsNewMinBackoff**: a rapid CaN is throttled so each generation lives ≥ `WorkflowIdReuseMinimalInterval` (1s default) — the successor's first WFT is delayed by `min_interval − lifetime`, lifetime measured from the run's EXECUTION time (start + its own first-WFT backoff, not start) so each generation gets a fresh backoff; a generation terminated during that window shows only `WorkflowExecutionStarted` (no scheduled WFT), which both `TestChildWorkflowWithContinueAsNewParentTerminate` and the run-execution-timeout leaf depend on. **(4) Child-CaN resolution fidelity**: the parent's `ChildWorkflowExecution*` event now references the FINAL run of the child's CaN chain (new `resolved_run_id` on `ChildResolvedRequest`, the resolving run's id, not the started run); and a parent-close-policy terminate/cancel targets the child's CURRENT execution (resolve by workflow id, not the stale started run id) so a child that continued-as-new is actually terminated (v1.31.0 targets `{WorkflowId}` with `FirstExecutionRunId` as an across-runs guard). **(5) Monotonic event task_id**: tokeira has no shard task-id generator, so each event's `TaskId` is synthesized from `happened_at`(nanos) + `event_id` — strictly increasing within a run and jumping across CaN generations (the min-backoff guarantees the gap), satisfying `TestWorkflowContinueAsNewTaskID`'s cross-run monotonicity. **(6) Internal-tq CaN block**: a CaN targeting the reserved internal per-namespace queue fails the WFT with a new `BadContinueAsNewAttributes` cause (appended LAST, postcard) via the K4 seam, mirroring the child leaf. |
| 3.14 | `TestChildWorkflowSuite` | ✅ **CLEAN — 8 pass / 0 fail / 0 skips** (3× stress, byte-stable; Tiers 1.1–2.13 re-verified unregressed; `cargo test --workspace` 118 binaries / 2099 tests green) | 2026-07-07 | Baseline 0/8 (one leaf hung to 300s). Seven leaves, grounded in v1.31.0 `transfer_queue_active_task_executor.go` (processStartChildExecution / startWorkflow) + `api/workflow_id_dedup.go` (ResolveDuplicateWorkflowID). **(1) Child WES parent/root/header fidelity** — the child's `WorkflowExecutionStarted` now authors `parent_workflow_namespace` (the NAME, carried on a new `parent_namespace_name` field resolved by the child orchestrator via `resolve_namespace_name`, since tokeira hashes the id one-way) + `root_workflow_execution` (was ignored — `root_workflow_id: _`); the parent's `ChildWorkflowExecutionStarted` event gained `namespace_id`/`namespace`/`header` (the sibling child resolution events already carried namespace) fed from a new `ChildWorkflowState.header`. **(2) Edge namespace default** — a child command that omits its own namespace inherits the WFT-completing worker's namespace NAME (`proto_command_to_workflow_command` gained a `default_namespace` param), else the empty name hashed to a bogus id and left every downstream `ChildWorkflowExecution*.Namespace/NamespaceId` empty. **(3) Namespace-id reconciliation** — tokeira's `DescribeNamespace` now reports its real (hash-derived) id instead of the unset stored id, and the conformance adapter (`tokeira_conformance_cluster.go` `CreateNamespace` + `functional_test_base.go` `RegisterNamespace`) adopts tokeirad's assigned id as `s.NamespaceID()` rather than the harness's throwaway UUID, so `event.NamespaceId == s.NamespaceID()` holds (fork change — the harness's arbitrary id is not part of the public contract). **(4) Retry/cron/CaN parent inheritance** — a child that retries (`build_retry_successor_start`) or continues-as-new/cron-restarts (lane successor) now propagates the full parent linkage (`parent_*`, root) to the successor run instead of hardcoding `None`, so each attempt's WES authors ParentWorkflowNamespace/Execution; and the parent child-resolution notify is guarded by `!close_continues_into_successor(...)` so a retrying child's non-final failure does NOT resolve the parent's future until `NewExecutionRunId==""` (MaximumAttemptsReached, transfer_queue_active_task_executor.go:387,422). **(5) Conflict-aware child start** — the publisher's `handle_start_child_workflow` now mirrors `ResolveWorkflowIDConflictPolicy`: a running same-id conflict under the default FAIL policy records `StartChildWorkflowExecutionFailed(WORKFLOW_ALREADY_EXISTS)`, while TERMINATE_IF_RUNNING (migrated to a TERMINATE_EXISTING conflict) terminates the running incumbent (`terminate_conflicting_incumbent`, `CurrentExecutionConflict` is reported only while the incumbent is open) and retries the start for a fresh run (`terminateWorkflowAction`, workflow_id_dedup.go:202-231). LEAF 7 (`TestStartChildWorkflowWithInternalTaskQueue_Blocked`) landed earlier via `BadStartChildExecutionAttributes` on the K4 seam. |
| 2.13 | `TestWorkflowBufferedEventsTestSuite` + `TestMaxBufferedEventSuite` | ✅ **CLEAN — 3 pass / 0 fail / 0 skips + 1 pass / 0 fail / 1 classified skip** (3× stress each; Tiers 1.1–2.12 re-verified unregressed; `cargo test --workspace` green) | 2026-07-07 | Baseline 0/5. Landed the **event-buffering Phase 2 activity slice** — the accepted `kernel-event-buffering` spec deferred Phase 2 "until a completion-during-started-WFT leaf demands it", and this tier is that demand (spec tasks 2.1–2.3, engine `<this commit>`; fork skip `<fork>`). (1) **`should_buffer` extended** to activity started + resolution events (`ActivityTaskStarted/Completed/Failed/TimedOut/Canceled`) and `WorkflowExecutionOptionsUpdated`, matching v1.31.0's whitelist-complement `bufferEvent` (event_store.go:263-318). Kernel emits route through a new `emit_or_buffer` (real id, or the shared `BUFFERED_EVENT_ID = -123` sentinel when a WFT is started). **The live activity-start path is the RUNTIME's direct-construct `start_activity_task` (the kernel `apply_activity_started` is vestigial), so it also had to become buffer-aware** — it buffers the `ActivityTaskStarted` (sentinel started id) when a WFT is started, dispatching the task anyway since its token is keyed on the scheduled event id. (2) **`flush_buffered` rewritten** with v1.31.0's `reorderBuffer` (stable two-way partition — resolution-class events flush AFTER all others, so a `*Started` always precedes its `*Completed`) + `wireEventIDs` (each resolution's `started_event_id` back-patched from the real id its buffered `*Started` received, keyed on the shared scheduled id; plus a durable back-fill of the activity's started id so a resolution arriving after the flush still wires). A buffered `WorkflowExecutionOptionsUpdated` flips its `RequestIdInfo` from `{buffered:true, event_id:0}` to the real id on flush (Describe assertion in `TestBufferedEvents`). (3) **Count limit** (`enforce_buffered_event_limit`, `MAX_BUFFERED_EVENTS`=100, the v1.31.0 default — not an override): the 101st buffered signal force-fails the started WFT with exactly one `WorkflowTaskFailed(FORCE_CLOSE_COMMAND)`, flushes, and reschedules a fresh normal task that redelivers all 101 signals; the workflow survives (`TestMaxBufferedEventsLimit`, `TestRateLimitBufferedEvents` → NotFound "Workflow task not found." on the stale completion). Skip: `TestBufferedEventsMutableStateSizeLimit` — needs `OverrideDynamicConfig(MutableStateSizeLimitError=410KB)` so 100KB signals exhaust mutable state at a test-sized threshold; unreachable at the v1.31.0 default 8MB and undeliverable out-of-process, so the size-limit terminate is deliberately not implemented (established OverrideDynamicConfig-class skip; the sibling count-limit leaf passes on the default). OPEN (Phase-2 remainder, not corpus-demanded here): child-workflow / Nexus / external resolution buffering (their tiers — the flush machinery is already general); dedicated kernel goldens for the activity-reorder spine (corpus is the byte-stable 3× verification). |
| 2.12 | `TestUpdateWorkflowSdkSuite` + `TestWorkflowUpdateSuite` + `TestUpdateWithStartSuite` | ✅ **CLEAN — 6 pass / 0 fail / 0 skips + 69 pass / 0 fail / 11 classified skips + 37 pass / 0 fail / 5 classified skips** (3× stress each, byte-stable; Tiers 1.1–2.11 re-verified unregressed; `cargo test --workspace` green) | 2026-07-07 | Biggest tier of the campaign (103+ leaves; baseline functional 8 pass under a panic mask). Landed as five waves. **Wave A** (`d2df062d`): send-once update delivery + accepted-set dedupe, `UpdateRejected` event removed (rejections are traceless — the event poisoned SDK replay with a TMPRL1100 nondeterminism panic loop), WFT-timeout zero-normalization (present-zero SDK durations defeated the 10s default → local-activity heartbeat busy-loop), unreferenced-message splice, abort taxonomy (AbortedByClosingWorkflow NotFound / ErrWorkflowClosing retryable / AcceptedUpdateCompletedWorkflow failure outcome). **Wave B** (`05798cb1`): `ExecuteMultiOperation` decision ladder (accepted multi-op spec). **Wave C — the speculative-WFT model** (accepted spec `.kiro/specs/speculative-wft/`, kernel pre-authorized; K+E `cff206f9`, R.2/3/4 `87d2ce05`, R.1 `d1f49d7f`, M.1 `3d9109a9`, close-out `10710ec1`; fork skips `9a9bd7bcb` + metrics bridge `b4befe147`): **(K)** `WORKFLOW_TASK_TYPE_SPECULATIVE` — attempt-1 no-trace tasks with virtual event ids; drop predicate (rejection-only ∧ ¬heartbeat ∧ events-window 0/10 by `discard_speculative_workflow_task_with_events`) rewinds the SDK via `ResetHistoryEventId`; late materialization otherwise; convert-to-normal on buffered signal / new-events-at-start; `BadUpdateWorkflowExecutionMessage` cause (appended LAST, postcard) + resurrect-from-AcceptedRequest; failure-capable `WorkflowExecutionUpdateCompletedV2` (old variant decode-only). **(R)** precise per-run in-memory timers (`SpeculativeTimerSet`) for the pinned 5s normal-queue STS + sticky STS + start-to-close — the 1s WFT-timeout sweep can't meet the corpus's ~100ms firing margin; poll discards superseded broker entries (Invariant I.1); stale completions → NotFound "Workflow task not found."; sticky-first dispatch with immediate normal-queue fallback when no sticky poller waits (`StickyWorkerUnavailable` swap, updateworkflow/api.go:224-233); server-decided WFT failures abort Sent waiters with WorkflowNotReady "…unexpected workflow task failure." while explicit `RespondWorkflowTaskFailed` keeps the update admitted + redelivers; RejectUnprocessed auto-rejects delivered-but-ignored updates with `unprocessedUpdateFailure` (kernel prunes via `delivered_update_ids` before the K7 follow-up decision, keeping admitted-set consistency); updates drain in admission order (`admitted_seq`). **(M)** namespace-labelled `speculative_workflow_task_commits`/`_rollbacks` + timer `task_requests`/`start_to_close_timeout` tagged `TimerActiveTaskSpeculativeWorkflowTaskTimeout`, kernel-signalled via metric dispatch ops, bridged in the fork. **Close-out** (`10710ec1`): worker rejection WINS over close-abort when one completion both rejects + closes (waiters pre-claimed before commit so the lane's close-drain can't abort first); consistent-query buffer pinned to v1.31.0's `MaxBufferedQueryCount` default **1** (was an invented 256) with typed `ErrConsistentQueryBufferExceeded` (RESOURCE_EXHAUSTED, BUSY_WORKFLOW/NAMESPACE) — tokeira needs no "clear WF context" analogue since the admitted update + pending speculative task are durable and delivery resumes without re-admission; empty sticky `worker_task_queue` tolerated as clear-stickiness; `WorkflowTaskScheduled` serializes `TaskQueue.Kind=NORMAL`. Recorded owner deviations (implementer-response.md): unknown-update NotFound implemented per accepted spec Req 6.2 (v1.31.0 wraps it in InvalidArgument at the frontend; corpus passes either way); never-admitted `Completed` takes the wasn't-found path. Skips: 11 functional + 5 UWS, all established registry classes (OverrideDynamicConfig / fault-injection / terminate-timing). OPEN (spec T/D phases, not corpus-visible): F2 re-arm-on-load (needs sweep queries to carry task_type/deadlines; single-node corpus never reloads mid-test), T.1 P1-P6/G1/G2 coverage audit + T.2 runtime/edge tests (goldens (a)-(e) + timeout shapes landed with K), broker grace-demotion exemption for speculative tasks (the precise timer reclaims a lost task before the 5s grace window), D.1 architecture-doc amendments. |
| 2.11 | `TestCancelWorkflowSuite` | ✅ **CLEAN — 5 pass / 0 fail / 0 skips** (3× stress; Tiers 1.1–2.10 re-verified unregressed) | 2026-07-05 | Baseline 1/4 (only `TargetNotFound` passed). Ultracode pattern (3-agent map + 3-lens adversarial review; the review found 1 real blocker — fixed — plus concerns, the cheap ones fixed, the rest documented here). (1) **Cancel of a closed or already-cancel-requested run is a success NO-OP** — no event, no WFT, RPC returns OK ("the request to cancel this workflow is a success even if the target workflow has already finished" + the `IsCancelRequested` short-circuit, requestcancelworkflow/api.go:44-53,74-81): kills the duplicate `WorkflowExecutionCancelRequested` on repeat cancels AND turns cancel-external-against-a-finished-target into the source's `ExternalWorkflowExecutionCancelRequested` SUCCESS event (the `TargetFinished` leaf) since the publisher's Applied maps to success. **Review blocker fixed alongside**: the lane's close side-effects (parent `ChildResolved` notify, CaN successor materialization) keyed on `new_state.closed_at` — the new noop commit against a closed run would re-fire them, and `apply_child_resolved` is keyed by child workflow id with no run fence, so a cancel hitting a closed child could resolve the parent's REUSED child id with the old incarnation's result; the close branch now requires a terminal event authored by THIS transition. (2) **`ExternalInitiatedEventId` threading**: the delivered cancel carries the initiator's initiated event id + `history-service` identity (transfer_queue_active_task_executor.go:1544-1567) — the target's `WorkflowExecutionCancelRequested` now shows `ExternalInitiatedEventId`/`ExternalWorkflowExecution` (the `TargetRunning` leaf asserted 5, got 0); direct-RPC cancels now record the caller's identity too (event_factory.go:578-590). (3) **`CancelWorkflow` command `Details` pass-through** onto `WorkflowExecutionCanceled` (was a unit variant; translate discarded the payload; event_factory.go:592-604). (4) **Start+RequestCancel same-child-same-WFT rejection** (K4 invalid-command seam, fifth cause): new `WorkflowTaskFailedCause::BadRequestCancelExternalWorkflowExecutionAttributes` (appended LAST — the enum is embedded in postcard-persisted `WorkflowTaskFailed` events and postcard discriminants are positional; NOTE Tier 1.7 inserted `BadRecordMarkerAttributes` mid-enum, a pre-existing upgrade-decode debt for pre-1.7 histories) + the exact v1.31.0 messages for the pair check and WorkflowId-not-set (command_attr_validator.go:250-293; batch-local children discriminated by `initiated_event_id > workflow_task_completed_event_id`, exactly v1.31.0's `initiatedChildExecutionsInBatch`). Atomicity was already right: one TransitionBuilder per completion, so the reject discards the StartChild event + dispatch — the child never exists (leaf asserts Describe→NotFound). (5) **`workflow not found for ID: <id>`** NotFound message (GetCurrentExecution rewrite, execution_manager.go:855-859; repo-wide grep found no consumer of the old `{ns}/{wf}` text). (6) **Publisher fidelity**: self-cancel guard (ns+wfid both match → pre-delivery Failed, transfer_queue_active_task_executor.go:546-559 — run id deliberately ignored), canonical `EXTERNAL_WORKFLOW_EXECUTION_NOT_FOUND` cause string (the Failed event's cause enum serialized UNSPECIFIED before), nil target namespace now inherits the source namespace (handler default; also closes a self-cancel-guard bypass). (7) **Transient-suffix on an EMPTY final history page**: the synthesized attempt>1 `WorkflowTaskScheduled` suffix keyed on the page's last event id and vanished when the real events exactly filled `MaximumPageSize` (the leaf's 5-event history vs the harness's page size 5); the suffix now keys on the reader's position (token fallback), matching v1.31.0's append-on-last-page (get_history_util.go:236-248,438-457). OPEN (review-noted, not corpus-visible): cancel-external attribute validation is partial (no RunId-UUID/length checks, no cross-namespace gate — v1.31.0's `system.enableCrossNamespaceCommands` defaults FALSE and the suite overrides it TRUE; tokeira allows cross-ns unconditionally, matching the enabled state); command `Reason`/`ChildWorkflowOnly` still dropped at translate (delivery-time parent check unimplemented; delivered reason stays synthesized); run-id-pinned lookups reuse the current-execution NotFound message and the direct cancel RPC ignores `run_id`/`first_execution_run_id`; the success-noop still commits a seq-bumping state rewrite (v1.31.0's `Noop:true` writes nothing — `StateTransitionCount` could diverge if a future leaf pins it); missing-namespace failures render `EXTERNAL_WORKFLOW_EXECUTION_NOT_FOUND` not `NAMESPACE_NOT_FOUND` (hash-derived namespace ids can't distinguish). |
| 2.10 | `TestQueryWorkflowSuite` | ✅ **CLEAN — 9 pass / 0 fail / 1 classified skip** (3× stress; Tiers 1.1–2.9 re-verified unregressed) | 2026-07-05 | Baseline 2/8 + a test-side panic. Ultracode pattern again (3-agent map + 3-lens adversarial review; the review found 0 blockers, 4 concerns — 3 fixed pre-landing, 1 documented). (1) **Query admission guards** (v1.31.0 queryworkflow/api.go:116-143): closed-before-any-WFT-started → `WorkflowNotReady("Workflow execution closed before WorkflowTaskStarted event")`; first-WFT backoff outliving the caller's deadline → `WorkflowNotReady("Workflow task is not scheduled yet.")` (the caller's gRPC deadline now threads through `grpc-timeout` metadata — previously a fixed 10s); WFT attempt ≥ 3 → `WorkflowNotReady("Unable to query workflow due to Workflow Task in failed state.")`. All FAILED_PRECONDITION + `WorkflowNotReadyFailure` detail. (2) **Dispatch rule = v1.31.0's `safeToDispatchDirectly`**: a query buffers unless a WFT has COMPLETED and the run is closed-or-quiescent (previously: direct whenever no pending WFT — which shipped query tasks for runs no worker had ever seen). (3) **Typed `QueryFailed`**: a worker-failed query is INVALID_ARGUMENT + `QueryFailedFailure` carrying the worker's structured failure (both respond paths thread it; previously swallowed into an empty-success response). Query timeout → `DeadlineExceeded("query timed out before a worker could process it")`. (4) **Sticky query fidelity**: a delivery claimed by the sticky-preferred worker carries EMPTY history (SDK answers from cache — replayCount pin), any other delivery carries FULL history from event 1 (was: partial-after-previous-started, matching neither v1.31.0 mode); the broker now expires sticky deadlines BEFORE identity matching so a post-deadline claim is a full-history non-sticky delivery. The broken "eager inline query WFT" (empty history + queries map — a shape v1.31.0 never emits; crashed SDK workers) is gone: unblocked buffered queries direct-dispatch per QueryCompletionTypeUnblocked, and — review fix — queries buffered at run-CLOSE are now unblocked too (worker answers against the closed run by replay) instead of failing. (5) **Delayed starts no longer reserve a sync-match poller** (the reserved-start path requires a pending WFT; a delayed start arms the start-delay timer instead — this failed the whole Start RPC in the corpus) and a failed reserved delivery no longer fails the durably-committed Start. (6) Run-id-pinned queries resolve exactly that run (previously silently ignored), malformed run ids → InvalidArgument "Invalid RunId.". Skip: `TestQueryWorkflow_NonStickyMultiPageHistory` — needs OverrideDynamicConfig(MatchingHistoryMaxPageSize=2); sixth of the class. OPEN (review-noted, not corpus-visible): direct-query poll responses don't paginate history (no next_page_token — v1.31.0 pages at MatchingHistoryMaxPageSize); queries attached to a WFT that later fails strand to caller deadline (v1.31.0 fails them retryable ErrBufferedQueryCleared); deadline-less queries during backoff park instead of failing fast; QueryRejectCondition still unimplemented (no leaf here pins it). |
| 2.9 | `TestSignalWorkflowTestSuite` | ✅ **CLEAN — 11 pass / 0 fail / 0 skips** (3× stress + 4× flake-hunt; Tiers 1.1–1.8 re-verified unregressed) | 2026-07-05 | Baseline 3/9. Six fixes, adversarially reviewed by a 3-lens workflow before landing (the review found 2 real blockers, both fixed). (1) **Run-scoped request-id dedupe**: the durable dedupe table treated (namespace, workflow_id, request_id) as globally unique; v1.31.0's request ids live in each run's mutable state, so a request id reused against a NEW run (signal-with-start after the prior run closed — the corpus reuses one RequestId across sequential calls) is fresh (mutable_state_impl.go:2361-2398). Both stores fixed (memory + DSQL, whose INSERT became a takeover upsert). **Review blocker fixed alongside**: the closed-incumbent retry dedup that the old global check accidentally provided is now explicit — `resolve_conflict` consults the closed latest run's `request_id_infos` (STARTED-authored ids) before reuse policy, returning DedupRetried exactly like v1.31.0's handleConflict. (2) **`workflowCloseAttempted` gate**: an UnhandledCommand-bounced close sets a runtime-local bit (the in-memory mirror of v1.31.0's volatile ms field, set AFTER the WFT-failed persist per the review's race finding); signals arriving while the retry WFT is STARTED get v1.31.0's `ErrWorkflowClosing` — RESOURCE_EXHAUSTED, cause BUSY_WORKFLOW, scope NAMESPACE, exact message, typed detail on the wire (signal_workflow_util.go:63-70, consts/const.go:62-67). Gate self-heals stale bits on closed runs. SCOPE (review-noted, not corpus-visible — **v1.31.0-faithful, do NOT "harden"**): the node-local bit mirrors v1.31.0's *volatile* in-memory `workflowCloseAttempted` — a plain `MutableStateImpl` struct field (mutable_state_impl.go:191), NOT persisted, reset to false on every mutable-state reload/failover/cache-eviction; tokeira's owner-local bit is the same model, not weaker. Internally/kernel-dispatched external signals bypass the gate, also matching v1.31.0: its `ValidateSignal` gate runs only on the client-facing SignalWorkflowExecution / SignalWithStart / Update paths (signalworkflow/api.go:55, signalwithstartworkflow, updateworkflow/api.go:148) — never on internally-delivered signal-external. Making the bit durable or gating internal delivery would DIVERGE from v1.31.0. Safety is unaffected regardless: history is authority, so a bypassed signal is buffered and re-evaluated by the retry close WFT. (3) **First-WFT backoff respected by signals**: `apply_signal` no longer schedules a WFT while the start-delay timer is armed (cron initial interval / client start delay) — `CreateWorkflowTask := !IsWorkflowPendingOnWorkflowTaskBackoff` (signalworkflow/api.go:70-74); the backoff timer's fire schedules the first WFT. Kernel spec-completion of the accepted start-delay machinery. Review-noted divergence (not corpus-visible): v1.31.0's signal-with-start on an EXISTING backing-off run skips the delay. (4) **External-signal command fidelity**: the command's header now travels on the dispatch and lands on the target's `WorkflowExecutionSignaled` event; delivered identity is `history-service`; a SELF-signal (same namespace + workflow id, run id irrelevant) fails with `SignalExternalWorkflowExecutionFailed` before delivery (transfer_queue_active_task_executor.go:683-697, 1574-1604). (5) **Typed closed-run + reject errors**: `KernelRejected(RunClosed)` → NotFound "workflow execution already completed" (ErrWorkflowCompleted, consts/const.go:51) for every closed-run mutation; start/signal-with-start policy rejections now render v1.31.0's exact `WorkflowExecutionAlreadyStarted` messages per reason (running / reject-duplicate / allow-failed-only, workflow_id_dedup.go:95-129). (6) **Projection idle-backoff flake** (caught by stress: 2-of-3 failures at exactly 4.02s): the projection worker's idle backoff capped at 5s — longer than the corpus's 4s `WaitForESToSettle` visibility budget — so a long-idle partition slept through the ListOpen assert; cap now 1s. Also fixed en passant: Tier 1.8's `skip_serializing_if` on `Payload.external_payloads` broke the DSQL postcard codec's encode/decode symmetry (positional format) — caught by workspace-level codec proptests; attribute removed. |
| 1.8 | `TestGetHistoryFunctionalSuite` + `TestRawHistorySuite` + `TestRawHistoryClientSuite` | ✅ **CLEAN — 6 pass / 0 fail (×2 transition-history variants) + 0 pass / 0 fail / 1 classified skip + 2 pass / 0 fail** (3× stress each; Tiers 1.1–1.7 re-verified unregressed) | 2026-07-04 | Baseline 0/6 + 0/1 + 1/1. Five fixes. (1) **Backlog fairness livelock** (gated `_All` and any late-arriving poller): the control loop re-seeds a queue's drain budget as `share × completed polls in window`, but a PARKED long-poll counts only at completion — a queue whose first poller arrived after its task aged into the durable backlog (5s grace) sat at budget 0 forever (no drain → poll never completes → no polls counted → budget 0). Each parked poller now floors the effective drain budget (`workflow_waiter_counts`); unit test pins the zero-budget drain. (2) **Close-filter token override**: the gRPC translation force-emptied `next_page_token` for close-event-filtered responses; v1.31.0 keeps the token live while the run is open so the client keeps long-polling until the close event (getworkflowexecutionhistory/api.go:488) — the edge core's token semantics now pass through. Long-poll hold aligned to v1.31.0's 20s `HistoryLongPollExpirationInterval` (was 60s). (3) **Reset successor materialization** (gated `TestGetHistoryReverse_MultipleBranches`; the successor previously hung to run-timeout): the fork cut copied history INCLUSIVE of the WFT-finish fork event while dropping its command events (incoherent state, nothing pending, nothing dispatched); v1.31.0 rebuilds to `WorkflowTaskFinishEventId - 1` (resetworkflow/api.go:119) — the cut is now exclusive in both stores (memory + DSQL), and the lane's reset block submits `WorkflowTaskFailed(ResetWorkflow)` to the successor off-lane (`AddWorkflowTaskFailedEvent` w/ RESET_WORKFLOW, workflow_resetter.go), which authors the failure and re-dispatches a fresh task through the ordinary path. NOTE: v1.31.0 also clears the WFT attempt for RESET_WORKFLOW (workflow_task_state_machine.go:924-926 — real scheduled event, not transient); not pinned by these leaves, kernel-frozen, noted for the reset-suite tier's raise. (4) **External payloads carried end-to-end**: the domain `Payload` gained `external_payloads` (dropped at every proto boundary before — silent data loss for external-payload round-trips); `ExternalPayloadsEnabled` defaults TRUE in v1.31.0 so the stats are unconditional: Describe derives `ExternalPayloadCount`/`SizeBytes` from committed history via a serde-tree fold mirroring `proxy.VisitPayloads` (external_payload_size.go; search attributes naturally excluded — typed, never `Payload`). (5) Skip: `TestGetWorkflowExecutionHistory_GetRawHistoryData` — requires suite-level `SendRawWorkflowHistory=true` (default false), and the raw path REPLACES parsed History with blobs (api.go:101), so it cannot be honored unconditionally; established OverrideDynamicConfig-class registry skip (fourth of its class). |
| 1.7 | `TestWorkflowFailuresTestSuite` | ✅ **CLEAN — 3 pass / 0 fail / 0 skips** (3× stress; Tiers 1.1–1.6 re-verified unregressed) | 2026-07-04 | Baseline 0/3 (harness panic + missing validation + visibility parse error). Landed four fixes. (1) **Partial-history license**: `is_sticky_match` now requires sticky-QUEUE dispatch (dispatch queue ≠ run queue), not worker-identity affinity — the degraded empty-queue hint armed on every WFT start made a transient retry's normal-queue redelivery attach suffix-only history, and the harness panicked slicing `events[PreviousStartedEventId:]` (13 vs 7 attached); v1.31.0 trims only when the run's sticky queue is set (`setHistoryForRecordWfTaskStartedResp`, recordworkflowtaskstarted/api.go:272-278; `IsStickyTaskQueueSet`, api.go:418). The reserved-start sync-match path also stopped claiming sticky. (2) **Req B.6 timestamp fidelity**: late-materialized transient Scheduled/Started events stamp the task's ACTUAL schedule/start times via new `emit_at` — the leaf asserts the persisted started time equals the poll-observed one, ≥1s before the close event (`workflowTask.ScheduledTime/StartedTime` in `AddWorkflowTaskCompletedEvent`, workflow_task_state_machine.go:768-800). (3) **RecordMarker validation + wire-message model**: empty MarkerName fails the WFT with new `BAD_RECORD_MARKER_ATTRIBUTES` (`ValidateRecordMarkerAttributes`, command_attr_validator.go:194-211); kernel `Reject::InvalidCommandAttributes.message` became `Option<String>` mirroring v1.31.0's nullable `causeErr`, the runtime composes the wire message (`"{cause}: {causeErr}"` / bare cause per `workflowTaskFailedCause.Message()`) and persists it as the WFT-failed event's `ServerFailure` — the corpus asserts `"BadRecordMarkerAttributes: MarkerName is not set on RecordMarkerCommand."` verbatim while Tier 1.6's bare `"UnhandledCommand"` still holds. (4) **Visibility filter conjunction**: `split_top_level` is now quote- and BETWEEN-aware — the legacy list-closed conversion emits `CloseTime BETWEEN 'a' AND 'b' AND WorkflowId = 'x'` and the old `split_once(" AND ")` ate the range connective ("unsupported filter expression"). Sticky S4 (fail clears sticky, no attempt increment — workflow_task_state_machine.go:1010-1015) located but NOT demanded: these leaves never arm a real sticky queue; stays open. |
| 1.6 | `TestTransientTaskSuite` | ✅ **CLEAN — 2 pass / 0 fail / 1 classified skip** (3× stress; Tiers 1.1–1.5 re-verified unregressed) | 2026-07-04 | Baseline 1/2. Landed the **UnhandledCommand contract**: a close command (Complete/Fail/Cancel/ContinueAsNew) with buffered events REJECTS the completion; the runtime persists the `WorkflowTaskFailed(UNHANDLED_COMMAND)` transition (unless a transient attempt keeps failing on another cause — dropped to time out) and errors the call with `INVALID_ARGUMENT("UnhandledCommand")` — mirroring v1.31.0's handler/API split (`hasBufferedEventsOrMessages` close guards + respondworkflowtaskcompleted/api.go:455-485,739-742). This REVISED the K4 seam: the kernel no longer converts internally — it rejects with the cause, the lane boundary now preserves typed rejects (`KernelRejected` wrapper, display-compatible with every existing string matcher), and `complete_workflow_task` runs the persist-then-error contract; the invalid-cancel goldens moved to the reject contract and buffering property P5 now models worker-close-past-buffer as impossible. The buffered flush resets the retry to a real attempt-1 task (Tier-1.2 rule (i)) — the leaf's `WithExpectedAttemptCount(1)` pins it. Skip: `TestTransientWorkflowTaskHistorySize` — requires `OverrideDynamicConfig(HistorySizeSuggestContinueAsNew=20KB)`; established OverrideDynamicConfig-class registry skip (fork). |
| 1.5 | `TestUserTimersTestSuite` + `TestWorkflowTimerTestSuite` | ✅ **CLEAN — 1 pass / 0 fail + 2 pass / 0 fail, 0 skips** (3× stress each; Tiers 1.1–1.4 re-verified unregressed) | 2026-07-04 | Baseline: UserTimers already clean; WorkflowTimer 1/1 — `TestCancelTimer_CancelFiredAndBuffered` died on `kernel rejected command: unknown timer` (the timer fired mid-WFT, was appended immediately and removed from state, so the same WFT's CancelTimer found nothing). Landed the **event-buffering Phase 2 timer slice** (spec `kernel-event-buffering`; the deferral trigger — "until a completion-during-started-WFT leaf demands it" — met by this leaf): `TimerFired` now buffers during a started WFT (`should_buffer`), and `CancelTimer` on a fired-and-buffered timer DELETES the buffered `TimerFired` so only `TimerCanceled` reaches history (`GetAndRemoveTimerFireEvent` in `AddTimerCanceledEvent`, mutable_state_impl.go @ v1.31.0); with the buffer emptied the close-flush emits nothing and no spurious follow-up WFT is scheduled. A truly-unknown CancelTimer now FAILS THE WORKFLOW TASK with the new `BAD_CANCEL_TIMER_ATTRIBUTES` cause via the K4 invalid-command seam (previously a gRPC-level command reject). Two golden pins added (buffered-fired cancel spine; unknown-cancel WFT failure). |
| 1.4 | `TestStickyTqTestSuite` | ✅ **CLEAN — 2 pass / 0 fail / 0 skips** (3× stress; Tiers 1.1–1.3 re-verified unregressed) | 2026-07-04 | Baseline 0/2 (both leaves waited for a `WorkflowTaskTimedOut {SCHEDULE_TO_START}` event tokeira could never write). Landed the **sticky task-queue model** (raise `docs/HANDOVER-sticky-taskqueue.md` — retired to git history — S1–S3+S5 as one gating unit per Kiro's in-doc §7; S4 deferred to Tier 1.6): `StickyAffinity` carries the sticky queue + per-dispatch schedule-to-start timeout (the edge validated the queue name and discarded it); sticky-held WFTs dispatch ONTO the sticky queue with `PendingWorkflowTask.schedule_to_start_deadline` (attempt-1-guarded; empty-name pre-S1 encodings degrade to the sync-match hint); `WorkflowTaskTimeoutType::ScheduleToStart` accepted on an UNSTARTED task — real timed-out event, sticky cleared, attempt NOT incremented (`ApplyWorkflowTaskTimedOutEvent`, workflow_task_state_machine.go:263-268), fresh attempt-1 reschedule on the normal queue; `Command::ResetSticky` (real `ResetStickyTaskQueue` RPC) clears affinity while the dispatched task's deadline survives (leaf 2's exact pin). Runtime: the WFT timeout scanner gained a `ScheduleToStart` kind (lane post-commit tracks unstarted deadlines; recovery unchanged semantics). Two integration bugs found by the bar: `commit.rs` removed unstarted pending tasks from timeout tracking (start-to-close-era logic erasing the S2S entry), and the `ReturnNewWorkflowTask` eager-return claimed from the normal queue while the WFT sat on the sticky queue (broke Tier 1.2's heartbeating leaf until fixed — claims now follow the actual dispatch queue). |
| 1.3 | `TestActivityTestSuite` + `TestActivityClientTestSuite` | ✅ **CLEAN — 10 pass / 0 fail / 0 skips + 6 pass / 0 fail / 0 skips** (3× stress each; Tiers 1.1 + 1.2 re-verified unregressed) | 2026-07-03 | Baseline 2/8 + 0/6. Landed in two waves. **Non-kernel** (`e44b0b20`): present-zero SDK timeouts are UNSET not insta-deadlines (the dominant failure mode; `timer_sequence.go:268-271`, sdk `internal_event_handlers.go:616`) + `validateAndNormalizeTimeouts` derivation; retry backoff-delayed dispatch + retry-expiration classification (`retry.go:70-113`); durable `last_failure` bookkeeping (`activity.go:82`); scanner retry-on-timeout with `TemporalTimeout:<Type>` classification + terminal ScheduleToClose conversion (`timer_queue_active_task_executor.go:281-362`); typed `ErrActivityTaskNotFound` with the exact `consts/const.go:44-45` message; by-id force-completion (`respondactivitytaskcompleted/api.go:89-105`); Describe `heartbeat_details`. **Kernel raise K1–K4** (`docs/HANDOVER-activity-kernel-gaps.md`, retired to git history; Req 0 accepted by Kiro in-doc; landed incrementally `c2758f33`/`6616144d`/`61cdf155`/`9e4d46cf`): resolutions carry `retry_state`; `ActivityTaskTimedOut` carries the timeout failure incl. `last_heartbeat_details`; durable worker identity (start/heartbeat/retry) + Describe `LastWorkerIdentity` fallback (`workflow/activity.go:159-166`); `RequestCancelActivity` keyed by `scheduled_event_id` with durable `cancel_requested`, not-started immediate cancel (`ACTIVITY_ID_NOT_STARTED`), and the **fail-WFT-with-cause error-model seam** (`Reject::InvalidCommandAttributes` → WFT failure, `failWorkflowTaskOnInvalidArgument` @ v1.31.0). K3 verification also surfaced+fixed a worker-flagged non-retryable failure that retried (marker-string bug; `isRetryable` flag short-circuit, retry.go:139-147). Zero registry skips — both suites fully green. |
| 1.2 | `TestWorkflowTaskTestSuite` | ✅ **CLEAN — 8 pass / 0 fail / 1 classified skip** (3× stress; Tier 1.1 re-verified unregressed 3×) | 2026-07-03 | Baseline 4/5/0, no hangs (Tier-1.1 groundwork carried the regular-WFT + raw-history cases). Landed: the **transient-WFT model** (spec `transient-wft`, Req 0 accepted — supersedes Feature-2's per-attempt-event design): attempt>1 WFTs live off-history with virtual ids (`mutable_state_impl.go:2250`, `workflow_task_state_machine.go:322-597`), only attempt-1 fail/timeout persists its event, transient force-close writes nothing (`workflow/util.go:118-120`), buffered-flush + new-events-by-start conversions reset to attempt-1 real events, success materializes Scheduled+Started late, and poll/GetHistory synthesize the virtual suffix (CLI/UI excluded, `get_history_util.go:427`). Phase A (buffered-flush-resets-retry) by Kiro; Phase B kernel completion + runtime/edge synthesis by Claude. Remaining skip: `TestWorkflowTaskHeartbeatingWithEmptyResult` — permanent `OverrideDynamicConfig` out-of-scope (WFT heartbeat timeout; owner decision, no knob). |
| 1.1 | `TestWorkflowTestSuite` | ✅ **CLEAN — 32 pass / 0 fail / 2 classified skips** (3× stress) | 2026-07-03 | Session-start baseline: suite could not finish (history-pagination infinite loop read as a hang), then 5 pass / 29 fail. Landed: history empty-page-token (corpus-wide unblock), start-response status/link fidelity, request-id dedup + terminate-on-conflict, per-NS-TQ rejection, activity retry defaults, incremental eager-WFT history (`20388323`, `d3fd2b7a`, `2d534260`), kernel event-buffering Phase 1 + force-close ordering (`4f70f6dc`, spec `kernel-event-buffering`), OnConflictOptions attach history fidelity, RunKey/run-id wire split, search-attribute wire codec + banned-predefined admission. 2026-07-03: the workflow retry chain landed (spec `workflow-retry-chain`, implemented by Kiro; harness confirmation surfaced 4 conformance fixes — ServerFailureInfo retryability per retry.go:115, execution_time = start + first-WFT backoff in projection AND Describe, and the FixFollowEvents legacy-client rewrite the corpus exercises via a simulated 1.3.1 Java SDK) — `TestWorkflowRetry`/`TestWorkflowRetryFailures` unskipped and GREEN. Remaining skips: `multiOp` (MultiOperation deferred → `api-conformance-multi-operation`), `OnConflictOptions_failed_max_callbacks` (OverrideDynamicConfig class). |

## Tier-1 + compatibility infra — outstanding

- **`conformance-harness`**: the `tokeira-conformance` crate is **not yet built** — TestCluster
  fixture, WorkerPoller, ExpectedHistory matcher, the 121-RPC coverage manifest + gate, three-way
  reconciliation, report, CLI, CI wiring.
- **`temporal-compatibility`** (tasks.md, 9 top-level tasks open): matrix-completeness properties (3),
  kernel `cfg_feature!` adoption (4), edge `dispatch_rpc` adoption (5), the Buffa/connect-rust
  compatibility service (7), `tkr compat` (8) and `tkr ci` (9) CLIs, the Dagger compatibility module
  (10) + versioned build/lockfile policy (11), final verification (14). Release-process aspects feed
  [`infra.md`](./infra.md).
- **`temporal-compatibility-surface`**: ✅ complete — the queryable matrix spine both tiers consume.

## Active conformance specs (detail-by-reference)

- [`functional-test-order.md`](./functional-test-order.md) — the order to drive Temporal's functional
  suites (Tier-2) against `tokeirad`, in/deferred/out partition, plus the in-process metrics-capture
  analysis.
- [`edge-unimplemented.md`](./edge-unimplemented.md) — current flat indicator of public-edge RPCs
  answering `UNIMPLEMENTED`, split into in-scope gaps vs intentional. Generated from the edge handlers.
- `nexus-async-completion` — async Nexus completion (C4b blocker for Odori's durable path); Wave 0 done,
  Wave 1 handed to Claude (`docs/HANDOVER-nexus-async-completion.md`, retired to git history).
- `workflow-id-conflict-policy-concurrency` — done (conflict-policy concurrency; `TestNexusWorkflowTestSuite`).
- `temporal-functional-conformance/reference/FINDINGS.md` — deep per-cluster investigation, ground-truth
  citations, and the Implementer Mandate. **Status lives here; investigation detail lives there.**
