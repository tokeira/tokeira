apiVersion: 1

providers:
  - name: Tokeira
    orgId: 1
    folder: Tokeira
    folderUid: tokeira
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: {{ dashboards_path }}
