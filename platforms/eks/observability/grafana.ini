# Revision-bound Grafana configuration. Credentials are held separately in
# Secrets Manager; the live-acceptance slice verifies their delivery path.
[server]
http_port = 3000

[auth.anonymous]
enabled = false

[analytics]
reporting_enabled = false
check_for_updates = false
