imports_granularity = "Crate"
format_code_in_doc_comments = true
# Vendored third-party crates are byte-attested against their release
# SHA256SUMS — `cargo fmt --all` reaches path/patched dependencies, and it
# must never rewrite them.
ignore = ["vendor/"]
